# 安全政策

## 攻击面

**纯客户端静态站点：没有后端、没有账号、没有数据库，也不收集任何个人信息。**

- 业务代码里唯一的 `fetch` 是读自己 `public/` 下的两份音频清单；产物不向任何外部域名发请求。
- 没有埋点、没有 Cookie、没有第三方脚本。
- `localStorage` 里只有五项偏好（深色、声音、字幕、旁白朗读、是否看过操作说明）。进度、
  灯谜得分、写下的愿望根本不落盘。
- 海报在本机 Canvas 合成，编号是 `crypto` 生成的随机码，不含用户标识，也不可反查。
- 首页带 CSP（`default-src 'self'`，脚本只放行自身与构建时现算的内联脚本 sha256），
  托管侧另配 `X-Content-Type-Options`、`Referrer-Policy`、`Cross-Origin-Opener-Policy`
  与一份只做减法的 `Permissions-Policy`。

## 算漏洞的

- 把可控内容注入 DOM 或着色器（心愿模块的选词、清单文件的内容）；
- 绕过 CSP，或让页面加载到本仓库以外的资源；
- 构建流程被污染 —— 产物里混进了源码里没有的东西；
- 依赖链上的已知漏洞，且在这个项目里真的能被触发；
- 让页面泄露它本不该接触的数据（比如把 `localStorage` 内容送出去）。

## 不算漏洞的

- 需要用户自己在开发者控制台里粘贴代码才成立的；
- 扫描器给出的、在纯静态站点上没有实际影响的报告（缺 HSTS 预加载、缺 `X-Frame-Options` 之类 ——
  页面没有账号也没有表单，被嵌进 iframe 里也没有可骗着点的东西）；
- 旧浏览器不支持 WebGL 2 或 ES2022 导致打不开 —— 这是[已声明的基线](docs/DEVELOPMENT.md#环境)；
- 社工、钓鱼、物理接触设备；
- 对 Vercel、GitHub 等平台本身的问题 —— 请报给它们。

## 怎么报

**不要开公开 Issue。** 用 GitHub 的私密报告：仓库页 → **Security** → **Report a vulnerability**，
或直接访问 [Security Advisories](https://github.com/MrBaoboer/SunMao-Lantern/security/advisories/new)。
这条通道只有维护者看得到。

报告里带上这些，能省一个来回：

- 受影响的页面或步骤、浏览器与版本；
- 复现步骤，越具体越好；
- 你认为的影响 —— 攻击者能做到什么；
- 有 PoC 就附上，截图或一段控制台记录都行。

## 之后会怎样

这是一个人维护的项目，没有 SLA。收到后尽快确认，判定成立就修，修好之后在同一条私密通道里告诉你，
发布公告时会署你的名（不想署名请说一句）。在修好之前，请不要公开细节。

## 维护范围

只维护 `main` 分支和线上部署的那一份。旧提交与派生版本不在支持范围内 ——
产物是纯静态的，自己部署的话，重新构建一次就是最新的。
