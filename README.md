<div align="center">

<img src="docs/img/hero.png" width="860" alt="点亮之后的榫卯灯笼：绵纸透出暖光，地上落着一片万字纹的光斑" />

# 榫卯灯笼 · 国风流光

**十三根木条，一颗钉子也不用。**

打开网页，从十三根方料开始，一刀一刀开榫凿卯，<br />
把一盏能拆开的中式灯笼做出来，再点亮它。

### [打开就玩 →](https://sunmao.vercel.app)

约 8 分钟 · 不用注册 · 不用下载 · 手机也能玩

[这是什么](#这是什么) · [自己做一遍](#自己做一遍) · [怎么做的](#怎么做的) · [怎么操作](#怎么操作) · [跑起来](#跑起来) · [文档](#文档)

</div>

---

## 这是什么

一节能动手做的木工课，跑在网页里，面向**十岁往上的孩子和陪着他们的大人**。

课本上的榫卯是一张剖面图，博物馆里的榫卯是一件不许摸的展品，两头都说不清**木头为什么能这样咬住木头**。
这里的办法是让你自己做一遍：十八步四幕，每一步都能亲手拖、亲手锯，每一道槽都是当场从毛坯上减出来的。

| 幕 | 步 | 在做什么 |
|---|---|---|
| **开场** | A1–A2 | 一盏灯，为一年收尾 —— 不用钉子，它凭什么立得住 |
| **认识榫卯** | B1–B3 | 凸的叫榫，凹的叫卯。直榫穿过去，夹榫落下来 |
| **做骨架** | C1–C8 | 开槽、切榫、凿透眼、凿柱窝、削细颈，十三根木条合龙 |
| **装点年味** | D1–D5 | 选花纹、装格心、糊纸贴花，最后拆开看一遍 |

<div align="center">
<img src="docs/img/unlit.png" width="860" alt="还没点灯的成品：四面万字纹格心糊着绵纸，四角立柱与出头的榫，一颗钉子也没有" />
</div>

十八步走完就是它。灯还没点，木作看得最清楚：四角出头的榫、四面镂空的棂条、糊在里面的绵纸。

做完之后还有四件事：**点灯**（按住不放，把灯点亮）、**猜灯谜**（答对一个，灯亮一分）、
**写心愿**（挑一句吉利话，做成一张海报）、**挂起来**（挂到夜色里，最多六盏）。

---

## 自己做一遍

<div align="center">
<img src="docs/img/craft.png" width="420" alt="拖动凿子在顺枨顶面开出榫槽" />
<img src="docs/img/exploded.png" width="420" alt="三十六件构件的分层拆解" />
</div>

**加工是过程，不是一段动画。** 凿子压在哪条槽上，就只有那条槽在掉料；刀走到哪儿，料才少到哪儿。
横枨两头各一个透眼，隔着大半根料，第一个凿穿了，第二个也得等刀真的走过去才开始掉料。手往回拖，木头不会长回去。
刀够不着的地方（另一根枨、另一个端头）文案会明说「另一头同样一锯」。

**拆开看，五层三十六件。** 底盘 5、上面的框 4、立柱 4、格心与纸 12、装饰与灯芯 11。
可以单看某一层，也可以拉着滑块把整盏灯慢慢摊开。

**不拦人。** 旁白没念完可以往前翻，任务没做完也可以往前翻。需要动手的地方旁边一直留着「帮我加工」「帮我装上」——
走同一段动画、同一组音效，跳过的只是手感。连着三次装不上，吸附会自己放宽，帮助按钮自己递过来。

**跟不上的人也有路。** 旁白按 3.3–4 字／秒配速，单行不超过约三十字，讲道理的那几步再放慢一档；
生僻词带注音（「榫卯，sǔn mǎo」「枨，chéng」）；灯谜答错不扣分、不重来，也不用红色。

---

## 怎么做的

无框架、无后端、无构建期资源：一个 [three.js](https://threejs.org) 依赖，一份 Vite 配置，产物是纯静态文件。
下面五件事是这一版的主要做法，实现细节见 [DESIGN.md](DESIGN.md)。

### 几何是算出来的

基本模数 a = 12 mm，最小刻度 a/12 = 1 mm，每一个尺寸都过一道守卫 —— 不是整数毫米就当场抛错。

十三件木构件由「毛坯盒减去若干带工序标签的切除盒」生成，一共十三种工序：开槽、切榫、凿透眼、凿柱窝、开颈、刨装板槽……
每一道都是一次精确的布尔减法。于是三件事顺带成立：加工感不用画（CSG 标出哪些面是新切出来的，着色器据此提亮一档）；
穿模可以证伪（十七件两两求交，136 组配对重叠体积全为 0）；面数也压得住（十七件包络合计 1152 个三角形）。

### 料是跟着刀走的

判定分三层：**哪一条道**（既非进给轴、也非进刀轴的第三个轴上，刃尖要落在这条槽的范围里）、
**啃到多深**（自入刀面向里按刀数推进，只增不减）、**走到哪儿**（进给轴上只去掉刃尖真正扫过的那一段）。

第三层是横枨那两个透眼、两个柱窝的要害 —— 它们横截位置完全一样，只沿走刀方向分开，
少了它，第一个孔一凿完第二个就跟着成形。这条由 `npm run verify` 的 `[CARVE]` 钉住。

做法是把「刀在哪、走了多少」直接喂给 CSG，现算这一刀啃掉的那一块。工具只有锯、凿、刨三样。

### 取景是算出来的

每一步只声明「必须完整看到多大一块」，相机自己算该退到多远：水平与垂直各算一次，取远的那个。

界面占掉的画面也一并算进去 —— 旁白与坞的实际像素高由 `ResizeObserver` 量出来喂给三维。
刀身同样算进去：凿自刃口向上还有 43 mm，而工件才 12 mm 见方。
1440×900、390×844 与 844×390（手机横过来）走的是同一套反算。

### 音效是实时合成的

因为它必须可参数化 ——「两件一组时第二记高 2 个半音」「四根立柱到位音依次上行 +0/+2/+4/+6」，固定采样做不到。

全部由 WebAudio 现场算：白噪声逐样本生成，木头是一组模态比 `[1, 2.76, 5.4, 8.9]` 的衰减振子。
声音只有木头、纸、火三类物理来源，界面自己只留选中、完成、快门三记。

### 纹样是程序化的

两种纹样都由一组「二维线段 + 线宽」生成，再挤出成真实的镂空棂条：万字纹（默认）把卍字连续铺陈，
四臂钩转落在单元角点上，相邻单元自然接续；麻叶纹是六边形棂格加每格六出放射。

同一份线段数据同时供给灯上的棂条、地面的光斑、海报的脚线与选纹样的缩略图 —— 你选的纹样，四处永远是同一个。
首图里地上那片光斑不是贴图，就是格心当聚光灯遮罩烘出来的。

> `npm run verify` 会在 Node 端跑完 17 条几何断言；页面加载时也会跑一遍，右上角的「尺寸对照」可以随时调阅。

---

## 怎么操作

第一次进来会先讲一遍，右上角常留一个完整版。

| | |
|---|---|
| 两侧箭头 · `←` `→` · `空格` | 上一步、下一步 |
| 顶部小格 | 点一下跳到那一步；键盘走到哪一格就报哪一步 |
| 拖动 · 滚轮 | 转动、缩放。转到哪儿就停在哪儿，镜头不会自己跑回去 |
| `X` | 没有卷或坞挡着时，把灯笼拆开、调透明 |
| `Esc` | 收起菜单与大多数覆盖层；互动模块用左上角那枚返回 |

需要动手的步骤，底部中央会出现一个按钮；构件直接拖就行，每一次装配都只有一个合法方向 ——
呼吸的小箭头会指出来是哪个方向，推歪了会阻尼回弹，并说出这一步为什么只能那样推
（「柱子不能从上往下放 —— 沿着箭头，从外面横着推」）。

「怎么操作」「尺寸对照」「拆开看看」盖在这一步自己的坞上面，收起时底层原样回来 ——
在「选一个花纹」那一步查一眼快捷键，回来花纹还在。

界面默认浅色，右上角可以切到深色。夜里的场景（做完灯之后的那几步）本来就该是暗的，
界面会自动跟着变暗，但不动你存下的选择。

窄屏另有一套布局：两侧翻页挪到底部两角，屏幕两侧整片让给手指转动画面；工艺笔记改成顶部通栏、可折可滚。
手机横过来（竖向不足 540 px）再压一档排字与间距。刘海与底部横条由 `env(safe-area-inset-*)` 让开。

<div align="center">
<img src="docs/img/mobile.png" width="240" alt="手机竖屏下的第一步，整盏灯完整在画面里" />
</div>

---

## 跑起来

```bash
npm install
npm run dev
```

打开 <http://localhost:5173>。

```bash
npm run build      # 静态产物 → dist/
npm run lint       # ESLint
npm test           # 单元断言：模数守卫、CSG 内核、补间取消语义（node:test，无依赖）
npm run verify     # 几何闭合验算（Node 端，无需浏览器）
npm run size       # 产物体积门槛
npm run smoke      # 冒烟测试：真实浏览器走完十八步 + 四个模块
npm run check:code # 上面除冒烟外的五样（不进浏览器，一分钟以内）
npm run check      # check:code + smoke
```

`npm run smoke` 与 `npm run shots` 需要一次性装好浏览器内核：

```bash
npx playwright install chromium
```

冒烟测试用构建产物起一个静态服务，在 1440×900 与 390×844 两种画幅下各走完十八步，每一步取一次 canvas 的亮度标准差 ——
防的是「脚本没报错，但画面是一块纯色」。桌面画幅还会把加工、装配、C6 的一键六道工序、快速翻页、点灯、
猜灯谜、写心愿海报、挂灯各跑一遍最短路径。
沿途盯着控制台报错、HTTP 4xx/5xx、未捕获异常、几何验算与刀刃朝向，任何一条都判失败。
加 `--shots` 会把每一步截图写到 `.shots/smoke/`：

```bash
npm run smoke -- --shots
```

GitHub Actions 上两条作业：**工具链**（`npm run check:code`，在 Node 22.13 与 24 上各一遍）每个 PR 都跑，二三十秒；
**冒烟**（无头浏览器走完主线与四个模块）只在合并进 `main` 之后跑，也可手动触发，软件渲染十几分钟，跑挂时留下出问题那一步的截图。
分开的理由见 [ARCHITECTURE.md](docs/ARCHITECTURE.md#检查)。

本文档里的五张图是从构建产物里拍的，模型或界面改动之后重出一遍即可：

```bash
npm run shots
```

旁白解说稿同理，由源码里的真实文案生成：

```bash
npm run script    # → tools/vo-manifest.json → 旁白解说稿.md
```

纯静态产物，任何静态托管都能放。当前 Demo 在 Vercel：`npm run build && npx vercel deploy --prod`。
`vite.config.js` 里 `base: './'`，产物用相对路径 —— 放在子路径下（如 GitHub Pages 的 `/repo/`）也不用改配置。

**环境**：Node 22.13+ 或 24+（下限由 ESLint 10 划定；Vite 本身只要 20.19+）。
运行时只有一个依赖 [three.js](https://threejs.org)。

浏览器需要 WebGL 2（three 0.185 的渲染器只申请 `webgl2` 上下文，没有降级）与 ES2022，
建议按 Chrome / Edge 111+、Safari 16.4+、Firefox 113+ 这条保守基线。
毛玻璃在 Safari 18 之前只认 `-webkit-` 前缀，用到的两处都写了；`text-wrap: balance` 这类不支持时原样降级。

移动设备与低配机走减配档：关掉高光溢出与阴影，像素比压到 1.5，天球贴图减半 —— 两档的颜色完全一致。
上下文被系统回收时（移动端切后台常见）会等一次 `webglcontextrestored`，找回来就原地接着走，这一遍的进度也就留住了。

---

## 项目结构

```
src/
  core/       modulus 模数体系 · boxcsg CSG 内核 · parts 构件参数 · verify 几何验算 · state 全局状态
  render/     stage 舞台与取景 · lantern 装配体 · materials · geometry · lattice 纹样 · decor · fx
  interact/   assembly 单自由度约束装配 · machining 拖刀加工
  audio/      sfx 模态合成音效 · voice 旁白字幕 · bgm 背景音乐
  ui/         hud 界面层 · icons · styles/{tokens,base,chrome,controls,surfaces}.css
  app/        engine 分步引擎
  steps/      act1 / act3 / act4 十八步主线 · util 步骤共用工具
  modules/    m1-m2 点灯与灯谜 · m3-m4 心愿与挂灯 · vo 模块旁白与片尾
  util/       tween 补间与调度
  devshot.js  开发期截图辅助（生产构建摇树移除）
  main.js     唯一的装配处：new 出每一层，塞进共享的 ctx，再把步骤表交给引擎
  styles.css  样式入口，只做 @import
tools/
  unit.test.mjs         单元断言（node:test）
  verify-geometry.mjs   几何闭合验算（Node）
  smoke.mjs             冒烟测试（无头浏览器）
  size-budget.mjs       产物体积门槛
  shots.mjs             重出 README 的五张图
  vo-manifest.mjs       从源码导出旁白清单
  make-script.mjs       旁白解说稿排版
vite.config.js          构建配置、开发期两个中间件、生产 CSP 注入
eslint.config.js        ESLint 扁平配置
.github/                工作流、Issue 与 PR 模板
```

构建产物 835 kB（gzip 228 kB），其中 three.js 单独一块 619 kB（gzip 153 kB），其余全部代码加起来 216 kB。
`npm run size` 守着这三个数。产物里没有图片、字体或音频：图标是内联 SVG 线稿，favicon 与那层纸纹是 data-URI SVG。

旁白与背景音乐留了挂载口，仓库里不含音频文件：旁白放 `public/audio/vo/{步骤号}.mp3`，
音乐放 `public/audio/bgm/{曲名}.mp3`，各自在同目录 `manifest.json` 里登记 `{ "ids": [...] }` 与 `{ "files": [...] }`。
没有音频时，字幕按语速模型独立走完同一套内容，一句不少。
全片旁白见 [旁白解说稿.md](旁白解说稿.md)，已分段断句、标好语速与停顿，可直接交给配音。
音效不在此列 —— 它们是实时合成的，没有文件。

---

## 隐私与安全

写心愿模块不采集任何个人信息：没有输入框，愿望从现成的词里挑或拼，不登录，不上传。
海报在设备本地用 Canvas 合成，编号是本机 `crypto` 生成的随机码，不含用户标识，也不可反查。
全站没有埋点、没有 Cookie、没有第三方脚本 —— 生产产物里不出现任何外部域名，
唯一的 `fetch` 是读自己 `public/` 下的那两份音频清单。

本机 `localStorage` 里只存偏好（深色、声音、字幕、是否看过操作说明）。
进度、亮度、灯谜得分与写下的愿望不落盘 —— 下次打开一律从头开始。

产物首页带一份 CSP：`default-src 'self'`，脚本只放行自身与首页那段主题内联脚本的 sha256（哈希由构建时现算）。
托管侧另配 `X-Content-Type-Options`、`Referrer-Policy`、`Cross-Origin-Opener-Policy` 与一份只做减法的
`Permissions-Policy` —— 摄像头、麦克风、定位、传感器一律关掉。

---

## 文档

- [ARCHITECTURE.md](docs/ARCHITECTURE.md) —— 模块边界、数据流、加一步要改哪几个文件
- [DESIGN.md](DESIGN.md) —— 几何、主线与三维交互的实现说明
- [UI.md](UI.md) —— 界面设计语言：主题、令牌、组件、文案规则
- [TROUBLESHOOTING.md](docs/TROUBLESHOOTING.md) —— 跑不起来、画面不对、测试挂了怎么查
- [旁白解说稿.md](旁白解说稿.md) —— 全片旁白，可直接交给配音

---

## 参与

修 bug、改拗口的文案、补无障碍与浏览器兼容 —— 这几类最有用，直接提 PR。
要加依赖、换框架、加新模块，先开个 Issue 聊。约定与本地要跑哪几条检查见 [CONTRIBUTING.md](CONTRIBUTING.md)。

发现安全问题走 [SECURITY.md](SECURITY.md) 的私密通道，不要开公开 Issue。
参与讨论请遵守[行为准则](CODE_OF_CONDUCT.md)。

---

## 许可

双轨授权。**能跑的东西归 AGPL，念出来、写出来、画出来的东西归 CC。**

- **代码**（`src/`、`tools/`、`index.html`、构建与 CI 配置）—— [AGPL-3.0](LICENSE)
- **内容与素材**（课程编排、全部旁白与文案、`docs/img/` 的截图、
  纹样与装饰件设计）—— [CC BY-NC-SA 4.0](legal/CC-BY-NC-SA-4.0.txt)

这是一个纯客户端应用，代码每次访问都完整送到浏览器里，AGPL 在这里是实打实生效的：
**部署出去就得让访问者拿得到完整源码**，你的修改也要同样以 AGPL 发布（压缩后的 `dist/` 不算源码）。
素材那一层是 NC，禁止商业使用。

想闭源、去掉署名、或原样拿课程内容做收费产品，需要一份单独的商业授权 —— 见 [COMMERCIAL.md](COMMERCIAL.md)。

Copyright © 2026 MrBaoboer
