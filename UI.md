# 界面设计语言

灯笼是唯一的主角。界面是一层极薄的东西，铺在它上面，随时准备退开。

这份文档是界面的唯一出处：改样式先改令牌，加组件先看这里有没有现成的。

---

## 五条规矩

**一屏只有一个金色。** 金色标记的是「当下唯一该按的地方」。多一个，它就什么也不标记了。

**信息按需出现，用完即走。** 同一时刻屏幕上最多四样东西：走到哪了、正在做什么、正在说什么、这一步要做什么。笔记、提示、标注都是任务性的，任务结束就消失。

**不拦人。** 旁白没念完可以往前翻，任务没做完也可以往前翻，任何一步都能直接跳过去。教学页面里，「被拦住」的伤害远大于「少练一次」。

**先说清楚怎么玩。** 三维拖拽不是不学就会的事。第一次进来先给四条操作，之后再也不打扰；右上角常留一个完整版。

**不用 emoji。** 它们在每个系统上长得都不一样，也压不住这套调子。所有图形都是同一套线稿图标（`src/ui/icons.js`）。

---

## 进入

页面只有一个入口：封面。

加载时它是一块实底，中间一条细线走满；三维就绪后换成一层薄纱，题字与入口浮在上面。

封面只交代两件事：这是什么，从哪儿进去。

| | |
|---|---|
| **主按钮** | 「开始做灯」。每次打开都是新的一遍，不问「要不要接着上次」 |
| **一个文字入口** | 「怎么操作」 |

按下主按钮同时做成三件事：淡出封面、解锁音频（浏览器要求真实手势）、进入第一步。
第一次进来会先展示一次「怎么操作」，之后不再出现。

**每次打开都是新的一遍。** 只有深色、声音、字幕这些偏好跨会话留着；做到哪一步、灯亮没亮，刷新即归零。
封面因此只有一个入口 —— 八分钟的东西不值得在门口先问一句「要不要接着上次」。

---

## 主题

默认浅色。右上角可以切到深色，选择存在本地。

组件只引用语义令牌，不直接写颜色 —— 于是换主题就是换一组 `:root` 变量，组件一行不用改。

| | 浅色 | 深色 |
|---|---|---|
| `--bg` `--surface` | 宣纸白，浮起的面更白 | 近黑，浮起的面略暖 |
| `--text` `--text-2/3/4` | 深墨，逐级变淡 | 纸白，逐级变淡 |
| `--accent` | `#8a6520`（压深一档，白字压金与金字压纸都过 4.5:1） | `#d3aa63` |
| `--line` `--shade` | 墨的低透明度 | 纸的低透明度 |

另有三个语义色，两套主题下各自只说一件事，绝不混用：

| | | |
|---|---|---|
| `--jade` | 青灰 | 这一件不动 |
| `--violet` | 紫灰 | 这里稍后要挖空 |
| `--seal` | 印泥红 | 只用于印章 |

**场景压过主题。** 夜里的场景（D5 与其后的互动模块）本来就该是暗的。走到那几步时界面临时切成深色，否则文字压在黑画面上根本读不出来。这不改用户的选择，只是暂时借用另一套颜色（`hud.setTone()`）。
三维工作台的光也跟着主题走：浅色模式下环境光更足、辉光更弱；深色模式下是暖调的暗房。

便签是唯一始终浅色的面，用一层 SVG 噪声（`--grain`）压出纸感，避免看起来像一块塑料。

---

## 字

- **衬线** 用于旁白、标题、谜面、灯笼上的字 —— 一切「被念出来」的内容。
- **无衬线** 用于界面本身：按钮、提示、标签。
- **等宽** 只用于数字与键帽：步骤计数、尺寸、件数。

字号是一条比例（`--fs-mark` … `--fs-boot`），不再出现 12.5px 这种数。
中文标题的字距会在末字后留一个空格，用 `text-indent` 抵消。

---

## 间距与形状

间距是 4 的倍数：`--s1` 到 `--s8`。屏幕安全边距 `--edge` 随视口收放。

圆角五档：`3px` 纸片，`6px` 卡片，`10px` 备用，`14px` 大面，`999px` 按钮与胶囊。
描边一律是 1px 内阴影（`--hair`）而不是 border —— 不占布局，也不会撑大盒子。

命中区与视觉大小分开算。顶部进度格视觉上只有 2px 的一道线，命中区留到 22px 高；图标按钮 38px。

---

## 动效

四档时长：`140 / 260 / 480 / 720ms`。缓动只有三条：`--ease` 收尾、`--ease-in-out` 转场、`--ease-spring` 盖章。

移动只发生在最小的那一层：翻页按钮悬停时动的是里面那枚箭头，不是整个圆 —— 圆一动，边缘就会抖。

`prefers-reduced-motion: reduce` 时全部动画压到 1ms。

翻页时 `cancelAll()` 会掐断所有在跑的动画和等待 —— 上一步排在后面的动作不该落到下一步的画面上。

---

## 组件

### 导航

| | |
|---|---|
| **两侧箭头** `.nav` | 贴着屏幕边缘，垂直居中。首尾自动置灰。任务做完时右边那枚会亮一下 |
| **章节** `.chapters` | 四章（开场 · 认识榫卯 · 做骨架 · 装点年味），每一章底下是这一章的几步。悬停显示步骤名，点一下跳过去。它同时是进度条和目录 |
| **步骤位置** | `03／18` + 步骤名。等宽数字在前，衬线标题在后 |

窄屏（≤680px）下两枚箭头落到底部两角，屏幕两侧整片让给手指转动画面。

### 底部

| | |
|---|---|
| **操作提示** `.cue` | 一行小字，配一枚手势图标。`<em>` 标动作词（金），`<b>` 标计数（等宽） |
| **旁白** `.narration` | 全片唯一的衬线阅读行 |
| **任务按钮** `#btn-task` | 只有需要动手的步骤才有，翻页不靠它。同一时刻至多一个 |
| **次要行动** `.alts` | 无框文字，最多两个。「帮我装上」这类降级入口都在这里 |

### 其他常驻件

| | |
|---|---|
| **工艺笔记** `.note` | 右上角一张纸。规格行用点线牵住，比表格轻，比并排清楚。不吃指针事件，可以隔着它转动画面 |
| **短提示** `.toast` | 画面中上方，衬线，带底 —— 灯亮起来时也读得清 |
| **返回** `.backbtn` | 左上角一枚，互动模块里唯一的出口。原先每个模块各自在控件行里塞一枚「回去」，位置随排版飘；退出这件事不该每次都去人群里找。层级压在覆盖层之上，卷摊开时也按得到 |

窄屏下笔记折成右缘一枚纸角，点开从顶部落下，不遮挡底部。内容一句不减。

### 更多菜单

右上角一枚 `⋯`。分四组，一组一件事：

```
怎么操作
———
深色 · 声音 · 字幕 · 旁白朗读
———
拆开看看 · 尺寸对照
———
从头再来
```

「旁白朗读」只在真的有配音文件时出现 —— 一个什么也控制不了的开关，比没有更糟。
菜单靠「点到菜单以外」收起，判定必须排除菜单自身：`pointerdown` 一旦把菜单摘出 DOM，随后的 `click` 就落不到开关上了。

### 底部这一摞是可叠加的

坞摊开时，界面层实测它的高度写进 `--dock-h`，底部的讲述与行动整体抬到坞上方
（`padding-bottom: max(基准, --dock-h + 间距)`）。

这不是细节。在此之前，纹样选择、分层拆解、四个模块入口和三个互动模块的旁白，
全都直接压在自己的控件上 —— 两层文字叠在一条横线上，谁都读不成。

同一份实测还交给三维（`hud.onSafeArea` → `stage.setSafeArea`）：
灯笼据此往上让、往后退，保证在坞上方仍然完整。**界面占掉多少，画面就让出多少。**

底部的字随时可能压在灯笼身上，而渐隐的暗角够不到那么高。所以 `.cue` 与 `.narration`
各自带一圈与底色同色的柔光（`text-shadow`）—— 浅色深色都成立，也不必为了读字去压暗画面。

### 覆盖层

只有两种形态：

**卷** `hud.sheet()` —— 盖住画面，讲一件需要专心看的事。
结构固定：眉标 → 标题 → 引言 → 正文 → 行动区。行动区只放**这一页要做的事**；
「退出」不在里面 —— 它固定在左上角那枚 `.backbtn` 上（见下）。
卷是模态的：焦点自动落到主行动上，`Esc` 关闭，方向键不会在它背后翻页。

**坞** `hud.dock()` —— 停在底部，把画面完整让出来。用于纹样选择、分层拆解、点灯亮度这类「一边看一边调」的场合。
坞不夺焦点，也不拦方向键 —— 手还在画面上。

两者都从声明式的参数生成，模块不再各写各的 HTML：

```js
hud.sheet({
  eyebrow: '第 3 题 / 共 5 题 · 打一日用物',
  title: '严丝合缝',
  lede: '这盏灯的每一个尺寸，都是同一个数算出来的',
  body: '<div class="spec">…</div>',
  actions: [
    { label: '再来一次', kind: 'primary', ico: 'refresh', on: retry },
  ],
  onEsc: close,
});
```

坞里的选项**选中即定案**。纹样选完直接进入下一步，不再多一个「就用这个」——
那个按钮既容易被卡片挡住，也没有存在的必要。

坞压得越矮，画面就越大。纹样二选一做成横条而不是大卡片、分层拆解把五个层名与滑杆并成一行，
都是同一条取舍：这两步真正要看的是背后那个东西，控件只需说清「有几个、选哪个」。

### 三维锚定

`.spot` 是钉在模型上的圆点标注，点开显示一张纸质小签。徽记用汉字（榫、卯、头、颊、肩、窝）或线稿图标。

`.arrow` 是方向引导，呼吸式明灭。**凡是要用户拖的地方，都有。**
没有它，画面上就只是一堆木头 —— 三维拖拽不是不学就会的事，「往哪个方向使劲」必须画出来。
所以它不由步骤各自摆放，而是由 `DragAssembly.begin()`、`Machining.begin()` 与 `simpleDrag()`
按各自的合法方向自动钉上：待装构件后方一枚指着它该去的方向，走刀线的另一端一枚指着该往哪儿拉。
装好一件就收掉一枚。夹榫那一步例外地在槽的两侧各来一枚 —— 叉口与槽都藏在两块料之间，
单看画面看不出该往下压。

**转到哪儿就停在哪儿。** 相机原先有一条「松手三秒自动缓回推荐机位」。
本意是别让人迷路，实际效果是你刚把画面转到看得清的角度，三秒后它自己溜回去 ——
动手的步骤里尤其难受：手上正在对位，画面在漂。这条**整条去掉了**，全片一致。

于是相机只在两种时候动：翻页，以及一步之内换工件（凿槽 → 截中梁 → 横枨三道工序）。
两者都是作者下达的转场，走的是 `setRecommended()`，它会清掉「用户接管过」这个标记。

还有两处会偷偷把画面推走，都堵上了：

- `setRecommended()` 顺手清标记 —— 而底部提示一换字数，`ResizeObserver` 就会经
  `setSafeArea()` 绕到它。内部重算因此要传 `{ keepUser: true }`：那只是拿旧声明
  重算距离，不是新的取景意图。
- 即便标记保住了，安全区一变推荐机位本身也会挪。动手的步骤里 `stage.hold(true)`
  连这次重算一起冻住，字幕换行不再让画面缓慢地飘。

签是不透明的纸，压在模型上。所以钉一枚之前先过三关：

**它说的是旁白没说过的话吗？** 重复一遍旁白，等于拿一张纸盖住画面去念字幕。
**它挨着自己指的东西吗？** 悬在构件上方一大截的圆点，与它指的那根木条之间没有任何视觉联系，
看到的只是一个不知所指的圆圈 —— 比没有更糟。
**它会盖住手要去的地方吗？** 这一条最要命：夹榫那一步曾经并排钉了两枚常开的签，
两张纸在屏幕上直接叠在一起，而且正好盖住要把叉口拖进去的那道槽。

过不了这三关的，删掉。同一时刻**至多一张签是摊开的** —— 点开新的会自动收起旧的
（`active: true` 是绕过这条的，因此只用于全场唯一的那一枚）。
窄屏与靠近右缘时签自动翻到锚点左侧展开，纵向与工艺笔记重叠时右边界收到笔记卡的左沿。

---

## 文案

**只写必要的。** 一行提示如果读者不看也不会做错，就删掉。

**说人话。** 「拖动刻刀，沿着槽来回走」，不是「请使用刻刀工具沿凹槽方向执行往复操作」。

**不自我解释。** 界面不解释自己为什么这样设计，不为难度道歉，不替用户表达感受，也不主动交代「我没有上传你的数据」这类没人问过的事 —— 那些写进 README 就够了。

**动词开头，一句一件事。** 按钮是「帮我装上」「再拆一次」「存下来」，不是「点击此处自动装配」。

**数字用等宽，量词跟着中文走。** 「短料 9 根 · 长料 4 根」。

**不写「左边那根」。** 画面随时能被拖着转，左右当场就作废。指代一律按构件长什么样 —— 「凸出来的那根」「带榫头的那根」。上下是例外：轨道控制不允许翻滚，`+Z` 永远朝上。

**错了不罚。** 猜错灯谜不扣分、不阻断、不重来，也不用红色 —— 红色在这套界面里只属于印章和「此处禁止」。

**同一句话只写一遍。** 模块旁白集中在 `src/modules/vo.js`，播放与导出配音稿取的是同一份；抄两处必然会漂移。

---

## 文件

```
src/styles.css                 入口，按顺序引入下面五个
src/ui/styles/tokens.css       令牌：浅色与深色两套语义变量
src/ui/styles/base.css         重置、排印基线、焦点圈、减弱动效
src/ui/styles/chrome.css       封面、章节、两侧翻页、笔记、旁白、任务
src/ui/styles/controls.css     按钮、滑杆、菜单、怎么操作、三维标注
src/ui/styles/surfaces.css     覆盖层：卷与坞，以及里面的门、签、谜、清单
src/ui/icons.js                线稿图标
src/ui/hud.js                  组件层
```
