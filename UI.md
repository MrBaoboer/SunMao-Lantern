# 界面设计语言

灯笼是唯一的主角。界面是一层极薄的东西，铺在它上面，随时准备退开。

这份文档是界面的唯一出处：改样式先改令牌，加组件先看这里有没有现成的。

---

## 四条规矩

**一屏只有一个金色。** 金色标记的是「当下唯一该按的地方」。多一个，它就什么也不标记了。

**信息按需出现，用完即走。** 同一时刻屏幕上最多四样东西：走到哪了、正在做什么、正在说什么、这一步要做什么。笔记、提示、标注都是任务性的，任务结束就消失。

**不拦人。** 旁白没念完可以往前翻，任务没做完也可以往前翻，任何一步都能直接跳过去。教学页面里，「被拦住」的伤害远大于「少练一次」。

**不用 emoji。** 它们在每个系统上长得都不一样，也压不住这套调子。所有图形都是同一套线稿图标（`src/ui/icons.js`）。

---

## 主题

默认浅色。右上角可以切到深色，选择存在本地。

组件只引用语义令牌，不直接写颜色 —— 于是换主题就是换一组 `:root` 变量，组件一行不用改。

| | 浅色 | 深色 |
|---|---|---|
| `--bg` `--surface` | 宣纸白，浮起的面更白 | 近黑，浮起的面略暖 |
| `--text` `--text-2/3/4` | 深墨，逐级变淡 | 纸白，逐级变淡 |
| `--accent` | `#9a7328`（压深，保证在浅底上的对比度） | `#d3aa63` |
| `--line` `--shade` | 墨的低透明度 | 纸的低透明度 |

另有三个语义色，两套主题下各自只说一件事，绝不混用：

| | | |
|---|---|---|
| `--jade` | 青灰 | 这一件不动 |
| `--violet` | 紫灰 | 这里稍后要挖空 |
| `--seal` | 印泥红 | 只用于印章 |

**场景压过主题。** 傍晚与夜里的场景本来就该是暗的。走到那几步时界面临时切成深色，否则文字压在黑画面上根本读不出来。这不改用户的选择，只是暂时借用另一套颜色（`hud.setTone()`）。
三维工作台的光也跟着主题走：浅色模式下环境光更足、辉光更弱；深色模式下是暖调的暗房。

便签是唯一始终浅色的面，用一层 SVG 噪声（`--grain`）压出纸感，避免看起来像一块塑料。

---

## 字

- **衬线** 用于旁白、标题、谜面、灯笼上的字 —— 一切「被念出来」的内容。
- **无衬线** 用于界面本身：按钮、提示、标签。
- **等宽** 只用于数字：步骤计数、尺寸、件数。

字号是一条比例（`--fs-mark` … `--fs-boot`），不再出现 12.5px 这种数。
中文标题的字距会在末字后留一个空格，用 `text-indent` 抵消。

---

## 间距与形状

间距是 4 的倍数：`--s1` 到 `--s8`。屏幕安全边距 `--edge` 随视口收放。

圆角只有四档：`3px` 用于纸片，`6px` 用于卡片，`10px` 备用，`999px` 用于按钮与胶囊。
描边一律是 1px 内阴影（`--hair`）而不是 border —— 不占布局，也不会撑大盒子。

---

## 动效

四档时长：`140 / 260 / 480 / 720ms`。缓动只有三条：`--ease` 收尾、`--ease-in-out` 转场、`--ease-spring` 盖章。

`prefers-reduced-motion: reduce` 时全部动画压到 1ms。

翻页时 `cancelAll()` 会掐断所有在跑的动画和等待 —— 上一步排在后面的动作不该落到下一步的画面上。

---

## 组件

### 导航

| | |
|---|---|
| **两侧箭头** `.nav` | 贴着屏幕边缘，垂直居中。首尾自动置灰。任务做完时右边那枚会亮一下 |
| **章节** `.chapters` | 四章，每一章底下是这一章的几步。悬停显示步骤名，点一下跳过去。它同时是进度条和目录 |
| **步骤位置** | `03／18` + 步骤名。等宽数字在前，衬线标题在后 |

### 底部

| | |
|---|---|
| **操作提示** `.cue` | 一行小字，配一枚手势图标。`<em>` 标动作词（金），`<b>` 标计数（等宽） |
| **旁白** `.narration` | 全片唯一的衬线阅读行 |
| **任务按钮** `#btn-task` | 只有需要动手的步骤才有，翻页不靠它。同一时刻至多一个 |
| **次要行动** `.alts` | 无框文字，最多两个。「帮我装上」这类降级入口都在这里 |

### 其他常驻件

| | |
|---|---|
| **工艺笔记** `.note` | 右上角一张纸。规格行用点线牵住，比表格轻，比并排清楚。不吃指针事件，可以隔着它转动画面 |
| **短提示** `.toast` | 画面中上方，衬线，带底 —— 灯亮起来时也读得清 |

窄屏（≤680px）下笔记折成右缘一枚纸角，点开从顶部落下，不遮挡底部。内容一句不减。

### 覆盖层

只有两种形态：

**卷** `hud.sheet()` —— 盖住画面，讲一件需要专心看的事。
结构固定：眉标 → 标题 → 引言 → 正文 → 行动区。行动区里主行动在右，退出永远是一行文字。

**坞** `hud.dock()` —— 停在底部，把画面完整让出来。用于纹样选择、分层拆解、烟花控制这类「一边看一边调」的场合。

两者都从声明式的参数生成，模块不再各写各的 HTML：

```js
hud.sheet({
  eyebrow: '第 3 题 / 共 5 题 · 打一日用物',
  title: '严丝合缝',
  lede: '这盏灯的每一个尺寸，都是同一个数算出来的',
  body: '<div class="spec">…</div>',
  actions: [
    { label: '再来一次', ico: 'refresh', on: retry },
    { label: '回去', kind: 'primary', on: close },
  ],
});
```

坞里的选项**选中即定案**。纹样选完直接进入下一步，不再多一个「就用这个」——
那个按钮既容易被卡片挡住，也没有存在的必要。

### 三维锚定

`.spot` 是钉在模型上的圆点标注，点开显示一张纸质小签。徽记用汉字（榫、卯、头、颊、肩、窝）或线稿图标。
`.arrow` 是方向引导，呼吸式明灭，只在需要指方向时出现。

---

## 文案

**只写必要的。** 一行提示如果读者不看也不会做错，就删掉。

**说人话。** 「拖动刻刀，沿着槽来回走」，不是「请使用刻刀工具沿凹槽方向执行往复操作」。

**不自我解释。** 界面不解释自己为什么这样设计，不为难度道歉，不替用户表达感受。

**动词开头，一句一件事。** 按钮是「帮我装上」「再拆一次」「存下来」，不是「点击此处自动装配」。

**数字用等宽，量词跟着中文走。** 「短料 9 根 · 长料 4 根」。

**错了不罚。** 猜错灯谜不扣分、不阻断、不重来，也不用红色 —— 红色在这套界面里只属于印章和「此处禁止」。

---

## 文件

```
src/styles.css                 入口，按顺序引入下面五个
src/ui/styles/tokens.css       令牌：浅色与深色两套语义变量
src/ui/styles/base.css         重置、排印基线、焦点圈、减弱动效
src/ui/styles/chrome.css       常驻界面：章节、两侧翻页、笔记、旁白、任务
src/ui/styles/controls.css     按钮、滑杆、菜单、三维标注
src/ui/styles/surfaces.css     覆盖层：卷与坞，以及里面的门、签、谜、清单
src/ui/icons.js                线稿图标
src/ui/hud.js                  组件层
```
