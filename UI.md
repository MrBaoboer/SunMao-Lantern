# 界面设计语言

灯笼是唯一的主角，界面只是薄薄铺在它上面的一层，随时准备退开。

改样式先改令牌，加组件先看这里有没有现成的。

---

## 五条规矩

**一屏只有一个金色。** 金色标记的是「当下唯一该按的地方」。多一个，它就什么也不标记了。

**信息按需出现，用完即走。** 同一时刻屏幕上最多四样东西：走到哪了、正在做什么、正在说什么、
这一步要做什么。笔记、提示、标注都是任务性的，任务结束就消失。

**一下按键，一段活儿。** 旁白没念完可以往前翻，顶上的格子随时能跳过去。但需要动手的步骤上，
「下一步」先用来做活儿：两条槽就是两下，四根柱子就是四下，做完最后一段才翻页。
教学页面里「被拦住」的伤害远大于「少练一次」，而一整步的动作一口气快放完，等于什么也没看见。
想整步跳过就点顶上的格子，或者用那一步自己的「帮我加工」「帮我装上」。

**先说清楚怎么玩。** 三维拖拽不是不学就会的事。第一次进来先给四条操作，之后再也不打扰；右上角常留一个完整版。

**不用 emoji。** 它们在每个系统上长得都不一样，也压不住这套调子。所有图形都是同一套线稿图标（`src/ui/icons.js`）。

---

## 进入

页面只有一个入口：封面。加载时它是一块实底，中间一条细线走满；三维就绪后换成一层薄纱，题字与入口浮在上面。

封面只交代两件事：这是什么，从哪儿进去。

| | |
|---|---|
| **主按钮** | 「开始做灯」 |
| **一个文字入口** | 「怎么操作」 |

按下主按钮同时做成三件事：淡出封面、解锁音频（浏览器要求真实手势）、进入第一步。第一次进来会先展示一次「怎么操作」。

做到哪一步、灯亮没亮，刷新即归零 —— 八分钟的东西不值得在门口先问一句「要不要接着上次」。
跨会话留下的只有界面偏好，界线见 [ARCHITECTURE.md](docs/ARCHITECTURE.md#状态)。

---

## 主题

默认浅色。右上角可以切到深色，选择存在本地。

组件只引用语义令牌，不直接写颜色 —— 换主题就是换一组 `:root` 变量，组件一行不用改。

| | 浅色 | 深色 |
|---|---|---|
| `--bg` `--surface` | 宣纸白，浮起的面更白 | 近黑，浮起的面略暖 |
| `--text` `--text-2/3/4` | 深墨，逐级变淡 | 纸白，逐级变淡 |
| `--accent` | `#8a6520`（压深一档，白字压金与金字压纸都过 4.5:1） | `#d3aa63` |
| `--line` `--shade` | 墨的低透明度 | 纸的低透明度 |

另有三个语义色，各自只说一件事，绝不混用：

| | | |
|---|---|---|
| `--violet` | 紫灰 | 这里稍后要挖空 |
| `--seal` | 印泥红 | 印章、「此处禁止」的标注、封面上的失败提示 |
| `--jade` | 青灰 | 留给「这一件不动」，暂未用到 |

**场景压过主题。** 夜里的场景（D5 与其后的互动模块）本来就该是暗的，
走到那几步时界面临时切成深色（`hud.setTone()`），否则文字压在黑画面上根本读不出来。
用户的选择不受影响。三维工作台的光也跟着主题走：浅色环境光更足、辉光更弱，深色是暖调的暗房。

便签是唯一始终浅色的面，用一层 SVG 噪声（`--grain`）压出纸感。

---

## 字

- **衬线** 用于旁白、标题、谜面、灯笼上的字 —— 一切「被念出来」的内容。
- **无衬线** 用于界面本身：按钮、提示、标签。
- **等宽** 只用于数字与键帽：步骤计数、尺寸、件数。

字号一律取自令牌表（`--fs-mark` … `--fs-boot`），不出现 12.5px 这种数。中文标题的字距会在末字后留一个空格，用 `text-indent` 抵消。

---

## 间距与形状

间距是 4 的倍数：`--s1` 到 `--s8`。屏幕安全边距 `--edge` 随视口收放。

常驻界面的四个边距一律用 `--pad-top/bottom/left/right`，
它们是 `--edge` 加上 `env(safe-area-inset-*)`。首页声明了 `viewport-fit=cover`，三维要铺满整块屏，
界面就得自己让开刘海与底部横条 —— 否则带刘海的手机上底部两枚翻页会压在小白条底下。

圆角只用三档：`3px` 纸片、`6px` 卡片、`999px` 按钮与胶囊；令牌里另有 `10px` 与 `14px`，暂时空着。
描边一律是 1px 内阴影（`--hair`）而不是 border —— 不占布局，也不会撑大盒子。

命中区与视觉大小分开算：顶部进度格视觉上只有 2px 的一道线，命中区留到 22px（触屏 32px，手机横屏 24px）；
图标按钮视觉 38px、命中区借 `::after` 撑到 46px；三维标注视觉 26px、命中区 44px；滑杆的轨仍是 1px，
触屏下把柄放到 20px、可按高度放到 40px。

非文本对比度按 3:1 收着：
进度格三种状态分别用 `--text-3`（未到达）、`--accent-mid`（走过）、`--accent`（当下，
另加高一格）。`--accent-soft` 压在纸上只有 1.5:1，不够用。

---

## 动效

四档时长：`140 / 260 / 480 / 720ms`。缓动只有三条：`--ease` 收尾、`--ease-in-out` 转场、`--ease-spring` 盖章。

移动只发生在最小的那一层：翻页按钮悬停时动的是里面那枚箭头，不是整个圆 —— 圆一动，边缘就会抖。

`prefers-reduced-motion: reduce` 时 CSS 动画与过渡一律压到 1ms。JS 驱动的那几段持续运动 CSS 管不到，
由 `reducedMotion()` 单独判：封面自转停掉，火焰跳动与流苏摆动压到两成，挂灯不再摇。
「可以往下走了」那个信号除了变金还多描一圈 —— 位移被关掉之后，只剩颜色就成了唯一靠辨色传达的信息。

翻页时 `cancelAll()` 会掐断所有在跑的动画和等待 —— 上一步排在后面的动作不该落到下一步的画面上。

---

## 组件

### 导航

| | |
|---|---|
| **两侧箭头** `.nav` | 贴着屏幕边缘，垂直居中。首尾自动置灰。任务做完时右边那枚会亮一下 |
| **章节** `.chapters` | 四章（开场 · 认识榫卯 · 做骨架 · 装点年味），每章底下是它的几步。悬停出步骤名，点一下跳过去 —— 它同时是进度条和目录 |
| **步骤位置** | `03／18` + 步骤名。等宽数字在前，衬线标题在后 |

**十八格只占一个 Tab 位。** 当前那一格 `tabindex="0"`、其余 `-1`，进来之后左右键在格子间移动、
`Home` / `End` 直达两头、回车跳过去。一格一个 Tab 位的话，键盘用户从页首走到「下一步」要按十九下 ——
而这一排是一件导航，不是十八个各自独立的目标。引擎的翻页键在焦点落到控件上时本来就不接管，两边不打架。

窄屏（≤680px）下两枚箭头落到底部两角，屏幕两侧整片让给手指转动画面。箭头的层级是 `--z-nav`（32），
必须压在底部那层渐隐暗角（`--z-chrome` 30）之上 —— 同层级时 DOM 顺序会让暗角把它整块糊掉，
而那是手机上唯一的翻页入口。三维标注与方向箭头同理，也在 `--z-nav`。

手机横过来走第三套：排字与间距各压一档，工艺笔记改成可滚并往里让出一个箭头的宽度。
这一档按**高度**判（竖向不足 540px），不按宽度 —— 844×390 在宽度上跟平板一样宽，竖向却只有它的一半。

### 底部

| | |
|---|---|
| **操作提示** `.cue` | 一行小字，配一枚手势图标。`<em>` 标动作词（金），`<b>` 标计数（等宽） |
| **旁白** `.narration` | 全片唯一的衬线阅读行 |
| **任务按钮** `#btn-task` | 只有需要动手的步骤才有，同一时刻至多一个。按「下一步」等于替他按它一下 |
| **次要行动** `.alts` | 无框文字，最多两个。「帮我装上」这类降级入口都在这里，与「下一步」走同一条路 |

坞摊开时，界面层实测它的高度写进 `--dock-h`，底部的讲述与行动整体抬到坞上方
（`padding-bottom: max(基准, --dock-h + 间距)`）。同一份实测交给三维（`hud.onSafeArea` → `stage.setSafeArea`），
灯笼据此往上让、往后退 —— **界面占掉多少，画面就让出多少。**
但字幕换一行不算「界面变了」：三维那边设了门槛，小变化不重新取景
（见 [DESIGN.md](DESIGN.md#6-取景算出来的不是摆出来的)）。

底部的字随时可能压在灯笼身上，而渐隐的暗角够不到那么高。
所以 `.cue` 与 `.narration` 各自带一圈与底色同色的柔光（`text-shadow`）—— 浅色深色都成立，
也不必为了读字去压暗画面。

### 其他常驻件

| | |
|---|---|
| **工艺笔记** `.note` | 右上角一张纸。规格行用点线牵住 —— 比表格轻，比并排清楚。宽屏上不吃指针事件，可以隔着它转动画面 |
| **短提示** `.toast` | 画面中上方，衬线，带底 —— 灯亮起来时也读得清 |
| **返回** `.backbtn` | 左上角一枚，互动模块里唯一的出口。层级压在覆盖层之上，卷摊开时也按得到 |

窄屏下笔记折成右缘一枚纸角，点开从顶部落下，不遮挡底部；这一档要能滚，指针事件就得收回来。内容一句不减。

### 更多菜单

右上角一枚 `⋯`。分四组，每组一类事：

```
怎么操作
———
深色 · 声音 · 字幕 · 旁白朗读
———
拆开看看 · 尺寸对照
———
从头再来
```

「旁白朗读」只在真的有配音文件时出现 —— 一个什么也控制不了的开关，比没有更糟。
菜单靠「点到菜单以外」收起，判定必须排除菜单自身：`pointerdown` 一旦把菜单摘出 DOM，随后的 `click` 就落不到开关上了。

### 覆盖层

只有两种形态：

**卷** `hud.sheet()` —— 盖住画面，讲一件需要专心看的事。结构固定：
眉标 → 标题 → 引言 → 正文 → 行动区，前三样都可以省。行动区只放**这一页要做的事**，
「退出」固定在左上角那枚 `.backbtn` 上。卷是模态的：焦点自动落到主行动上，`Esc` 关闭，
方向键不会在它背后翻页。

**坞** `hud.dock()` —— 停在底部，把画面完整让出来。用于纹样选择、分层拆解、
点灯亮度这类「一边看一边调」的场合。坞不夺焦点，也不拦方向键 —— 手还在画面上。

两者都从声明式的参数生成，模块不再各写各的 HTML：

```js
hud.sheet({
  title: '尺寸对照',
  lede: '这盏灯的每一个尺寸，都是同一个数算出来的',
  body: '<div class="spec">…</div>',
  actions: [
    { label: '知道了', kind: 'primary', on: () => hud.hideOverlay() },
  ],
  top: true,          // 盖在这一步自己的坞上面，收起时把它交还
  onEsc: () => hud.hideOverlay(),
});
```

**覆盖层分两层。** 底层归这一步自己（选花纹的坞、分层拆解的坞、四扇门），
上层归随时可能盖上来的那一页（怎么操作、尺寸对照、拆开看看，
传 `top: true`）。`hideOverlay()` 只收最上面那一层，底层原样回来；`closeOverlays()` 两层一起收，
只在翻页与「从四扇门进模块」这两处用。

这一条不能省：选花纹、分层拆解、四扇门这三步唯一的前进入口就是那个坞，一并收掉就是三条死路 ——
打开菜单看一眼怎么操作，回来坞没了。

坞里的选项**选中即定案**。纹样选完直接进入下一步，不再多一个「就用这个」。坞压得越矮，画面就越大：
纹样二选一做成横条而不是大卡片、分层拆解把五个层名与滑杆并成一行 —— 这两步真正要看的是背后那个东西，
控件只需说清「有几个、选哪个」。

### 三维锚定

`.spot` 是钉在模型上的圆点标注，点开显示一张纸质小签。徽记用汉字（头、颊、肩、窝）或线稿图标。

`.arrow` 是方向引导，呼吸式明灭。**凡是要用户拖的地方，都有。** 没有它，画面上就只是一堆木头 ——
「往哪个方向使劲」得画出来才知道。所以它不由步骤各自摆放，
而是由 `DragAssembly.begin()`、`Machining.begin()` 与 `simpleDrag()` 按各自的合法方向自动钉上：
待装构件后方一枚指着它该去的方向，走刀线的另一端一枚指着该往哪儿拉。装好一件就收掉一枚。
夹榫那一步例外地在槽的两侧各来一枚 —— 叉口与槽都藏在两块料之间，单看画面看不出该往下压。

签是不透明的纸，压在模型上。所以钉一枚之前先过三关，过不了的删掉：

- **它说的是旁白没说过的话吗？** 重复一遍旁白，等于拿一张纸盖住画面去念字幕。
- **它挨着自己指的东西吗？** 悬在构件上方一大截的圆点，读出来只是一个不知所指的圆圈。
- **它会盖住手要去的地方吗？** 夹榫那一步若并排钉两枚常开的签，两张纸会叠在一起，还正好盖住要把叉口拖进去的那道槽。

同一时刻**至多一张签是摊开的** —— 点开新的会自动收起旧的（`active: true` 绕过这条，
因此只用于全场唯一的那一枚）。窄屏与靠近右缘时签自动翻到锚点左侧展开，
纵向与工艺笔记重叠时右边界收到笔记卡的左沿。

**转到哪儿就停在哪儿。** 相机没有「松手几秒自动缓回推荐机位」这条 —— 手上正在对位、画面自己在漂，
比迷路难受得多。它只在作者下达的转场里动：翻页，以及一步之内换工件或换工序
（C2 的开槽与截中梁、C4 的横枨三道）。转场怎么走、什么时候该忍住不绕，
见 [DESIGN.md](DESIGN.md#6-取景算出来的不是摆出来的)。

---

## 文案

**只写必要的。** 一行提示如果读者不看也不会做错，就删掉。

**说人话。** 「拖动刻刀，沿着槽来回走」，不是「请使用刻刀工具沿凹槽方向执行往复操作」。

**不自我解释。** 界面不解释自己为什么这样设计，不为难度道歉，不替用户表达感受，
也不主动交代「我没有上传你的数据」这类没人问过的事。

**动词开头，一句一件事。** 按钮是「帮我装上」「再拆一次」「存下来」，不是「点击此处自动装配」。

**数字用等宽，量词跟着中文走。** 「短料 9 根 · 长料 4 根」。

**不写「左边那根」。** 画面随时能被拖着转，左右当场就作废。
指代一律按构件长什么样 —— 「凸出来的那根」「带榫头的那根」。上下是例外：
轨道控制不允许翻滚，`+Z` 永远朝上。

**错了不罚。** 猜错灯谜不扣分、不阻断、不重来，也不用红色 —— 红色在这套界面里只属于印章和「此处禁止」。

**同一句话只写一遍。** 旁白讲过的事，提示、笔记与标注就不要再讲一遍 —— 一条信息只占屏幕上的一个位置。

---

## 文件

```
src/styles.css                 入口，按顺序引入下面五个
src/ui/styles/tokens.css       令牌：浅色与深色两套语义变量
src/ui/styles/base.css         重置、排印基线、焦点圈、减弱动效
src/ui/styles/chrome.css       封面、章节、两侧翻页、笔记、旁白、任务
src/ui/styles/controls.css     按钮、滑杆、菜单、怎么操作、三维标注与签
src/ui/styles/surfaces.css     覆盖层：卷与坞，以及里面的门、谜、清单
src/ui/icons.js                线稿图标
src/ui/hud.js                  组件层
```
