# 《榫卯灯笼 · 国风流光》V3.0 · Web 重构说明

> 基准：《榫卯灯笼_国风流光_3D交互脚本_V2.0》（33 步 + M1–M5 + §2 二十五项几何校验）
> 本版：浏览器端可运行的完整交互实现，主线重构为 **24 步**
> 目标平台：Chrome（桌面横屏优先，移动端响应式可用）

---

## 0. 一句话

V2.0 是为「JigSpace 类分步 3D 讲解器」写的**制作执行脚本**，假设有建模、动画、配音、UI 四支队伍照着做。
V3.0 把它落成**一个真的能跑的网页**，并且做了一件原文档做不到的事：**让几何自己证明自己是对的**。

---

## 1. 最重要的一个决定：几何是算出来的，不是建出来的

原文档 §3 建立了模数体系（a = 12 mm），§5 定义了五个节点的全部参数，§13.1 列了 15 条建模自检。
这三章合起来其实已经是一份**可执行的规格**——只是它被写成了给人看的表格。

V3.0 把它翻译成代码：

| 原文档 | V3.0 实现 |
|---|---|
| §3 模数体系 | `src/core/modulus.js` —— 全部尺寸为整数毫米（a/12 = 1 mm），**零浮点误差**；`a()` 带栅格断言，任何非模数尺寸当场抛错 |
| §5 节点参数 | `src/core/parts.js` —— 13 件木构件由「毛坯盒 − 若干带工序标签的切除盒」参数化生成 |
| 布尔运算 | `src/core/boxcsg.js` —— 非均匀体素栅格 + 贪心面网格化。**不做实时布尔**（§S15 备注的要求），且天然无内部面 |
| §13.1 自检表 | `src/core/verify.js` —— 15 条断言，**页面加载即跑**，控制台输出报告，界面右上角「校验」可随时调阅 |

### 由此白拿的三件事

**① 加工感是算出来的。**
CSG 内核给每个顶点标了 `aCut`（该面是否为加工新露出的面）。木料着色器据此在同一个材质里同时表达 `MAT_WOOD_MAIN` 与 `MAT_WOOD_CUT`——铣一刀，新切面自动亮一档。§11.2 要求的「切面材质必须比外表面浅一档，这是加工感成立的关键细节」，不需要美术单独画。

**② 干涉检测是免费的。**
实体单元可以直接展开成不重叠盒体集合，于是「17 件构件两两是否穿模」变成一次集合运算。
**当前结果：136 组配对，重叠体积全部为 0。** 这是原文档 §2 反复强调、却只能靠人工验算的事。

**③ 面数极低。**
贪心面网格化后，一根开满槽的顺枨 **86 tris**（§11.1 预算 900），全部 17 件包络体合计 **1152 tris**（预算 50000）。省下的预算全给了格心镂空。

---

## 2. 本版新查出的两处文档内部矛盾

原文档 §2 的 25 项修正（V-01 ~ V-25）本版全部复核通过。把每个尺寸代入整数坐标后，**又发现两处 V2.0 自身的表述冲突**，按几何自洽性做了裁决：

### ★ V-26 · 中梁端部下半段：是「保留」还是「去除」？

- §5 J-2 承重面栏：「中梁端部**下半段保留为肩**，坐落在顺枨槽底余料上」
- S16 动画栏：「**切下半段肩**：自端部下半段横切**去除** a/2 高 × a/2 长的料」

两者冲突。代入坐标验算：顺枨开口槽只挖 z ∈ [2.5a, 3a]，槽底 z ∈ [2a, 2.5a] 是保留的余料。若中梁端部下半段也保留，两者在 y ∈ [3.5a, 4a] × z ∈ [2a, 2.5a] **正面干涉**。

**裁决：下半段必须去除。** 横切后露出的水平面（z = 2.5a，即双榫底面）落在顺枨槽底上——这个面才是承重肩。
文案已相应改写为：「端头下半段横切去除，切出的水平面即承重面」。

### ★ V-27 · 龙纹角花够不到压槽

- §5 J-6：「角花内腔 a × a × **a/2** 方腔，套住柱头段**上部**」
- §5 J-6：「压舌 a/6 厚 × a/2 长，落入**上枨框顶面**对应浅槽」

柱头段高 2a（z 14a–16a），上枨框顶面在 z = 14a。若内腔只有 a/2 高且套住柱头**上部**，角花底面在 z = 15.5a，**压舌够不到 z = 14a 的槽**，差 1.5a。

同时，原设计的锁定原理也不成立：内腔四壁若与柱头同域，柱头沿 +X 外拔时并没有东西挡它。

**裁决：改为带外挡边的方形盖板。**
- 板贴上枨框顶面（z ∈ [14a, 14a + a/3]），中开方孔套住柱头根部
- 压舌自板底面向下 a/6，落入横枨顶面压槽
- **外缘出挑 a/6**，正好横在柱头径向外拔的路径上

这样 §5 J-6 承诺的「结构副锁」才真正成立。相应地，`railB` 增加了 `PRESS_SLOT` 工序。

---

## 3. 主线重构：33 步 → 24 步

### 重构原则

原文档 §8 的 33 步累加 908 秒（约 15 分钟）。网页端 15 分钟线性流程的流失率极高，而且流失最严重的恰恰是**观看型**步骤——它们同时也是最依赖高成本写实美术资产的部分（老街低模场景、河姆渡考古构件、水墨意象）。

反过来，这个项目真正独一无二、且程序化几何最擅长表达的，是 **§2/§3/§5/§6 那套严丝合缝的模数几何**。

所以重构方向是一句话：**把「看视频」改成「看结构」。**

合并的全部是纯过场与重复劳动，**§13.4 的导演红线一条未动**。

### 步骤对照表

| V3.0 | 原步骤 | 说明 |
|---|---|---|
| **A1** | S00 + S01 | 封面与文化导入。老街写实场景 → **抽象「灯河」**：12 盏形制各异的灯笼剪影依次点亮，主角始终不亮。成本低得多，且天然满足原备注「禁止与主角同形制以免剧透」 |
| **A2** | S02 + S03 | 成品亮相 + 统一爆炸预演 + 五阶段路线图 |
| **B1** | S04 | 榫卯是什么 · 七千年 |
| **B2** | S05 + S06 + S07 | **榫卯解剖台**：同一场景里依次展开「凸/凹本质 → 榫三要素热点 → 卯三种规格」。原三步各自建场，合并后空间参照连续，反而更好懂 |
| **B3** | S08 | ★ **静默点①，完整保留，不与任何步骤合并** |
| **B4** | S09 + S10 | 直榫：讲解与亲手装配连做 |
| **B5** | S11 + S12 | 夹榫：双向嵌夹剖切 + 自上而下落入 |
| **C1** | S13 + S14 | 13 根木料展示与取料 |
| **C2 – C11** | S15 – S25 | **一步不减**。这是全片价值核心 |
| **D1** | S26 | 格心纹样选择（全片唯一个性化） |
| **D2** | S27 | 三段式落堂装板 |
| **D3** | S28 + S29 | 糊纸贴窗花 + 装饰件。合并为一步内的五个阶段按钮，节奏更紧 |
| **D4** | S30 | ★ **静默点②，12 秒巡礼全清屏** |
| **D5** | S31 | 分层爆炸复盘 |
| **D6** | S32 | 互动模块枢纽 |

净结果：**24 步，约 8–10 分钟**，知识点零损失。

### 导演红线核对（§13.4）

- [x] 静默点① B3：BGM 减至 0.35 编制，字幕改衬线体放大 15%，无操作任务
- [x] 静默点② D4：12 秒 `hud.quiet(true)` 全清屏，无 UI、无音效、无成就弹窗
- [x] C8 对比动画未删减：LB-A 中部双槽高亮 → UB-A 位置浮现红色 ✕（全片唯一使用 `#C8262B` 处）
- [x] D2 三段式装板未简化为一次性吸附：斜 12° → 顶入上槽 a/2 → 摆正 → 下落 a/4
- [x] D3 首个角花手动安装并播放剖切演示
- [x] D4 灯芯**不提前点亮**，`setLit(0)`
- [x] M5 片尾卡为 `13 根木条 / 0 颗钉子 / 7000 年`，未改写为通用祝福语
- [x] A1 开场钩子与 M5 片尾闭环
- [x] 语义色全片无误用（金/灰/紫灰/唯一红，见 `PALETTE`）

---

## 4. 新增：全局结构检视器

原文档把剖切散落在 S07 / S11 / S17 / S19 / S24 / S27 六处，每处都是一次一次性演示。

因为几何是程序化的，V3.0 把它升级成一个**常驻能力**：任意时刻按 `X`，可对当前模型做爆炸与半透，滑杆连续控制。

这是网页端相对分步讲解器的真正优势——不是「播放一段预制的剖切动画」，而是「随时可以拆开看」。

---

## 5. 音频：为什么音效是合成的

MiniMax 提供 T2A（配音）与 music_generation（BGM），但**没有音效接口**。这反而是好事，因为脚本本身要求音效是**参数化**的：

- S02 建立的 `SFX_SNAP_IN` 后续 12 次装配复用
- S20 第二记 `SFX_SNAP_IN`「音高高 2 个半音」
- S13 十三次落料「音高随机 ±2 半音」
- S25 四根立柱到位音「音高依次上行」
- S15 走刀音「音高随刀数升高」

**固定采样做不到这些，实时合成天然做得到。**

`src/audio/sfx.js` 用**模态合成**实现 60+ 音效。方法是物理导向的：敲击声 = 一个宽带瞬态 + 一组指数衰减的共振模态。木料模态阻尼大、余韵短（≤0.3 s，符合 §12.2 对听觉签名的要求）；铜磬模态非谐、余韵长（≥2 s）；锯/凿/铣是带通噪声加包络。这不是「像」，这就是这些声音的产生方式。

§12.2 优先送审的 6 个关键音效全部实现，含 M5 的 **0.25 s 声画延迟**（`SOUND_DELAY` 常量，触觉/视觉脉冲与**延迟后的声音**同步，而非与视觉同步）。

### 配音与 BGM

- `tools/gen-voice.mjs` —— 36 条旁白，音色按 §12.3 选用**温润男声**（明确避开「播报男声」，规范禁用播音腔），抒情段落切换抒情男声；「（气口）」「（停顿 n s）」自动转成 MiniMax 的 `<#x#>` 停顿标记；语速由各步 `cps` 折算
- `tools/gen-bgm.mjs` —— §12.1 的 8 首曲目，各带编制描述

**当前状态：MiniMax 账户余额不足（`1008 insufficient balance`），音频尚未生成。**
密钥本身有效（音色列表接口正常返回 303 个音色）。充值后跑：

```bash
npm run gen:voice && npm run gen:bgm
```

未生成时，字幕按 §12.3 的语速模型独立走完全同一套内容——这正是 §7「降级后叙事与知识点零损失」的兑现方式。

---

## 6. 交互：§6 自由度表的直接实现

§6 明确要求「所有拖拽交互一律做成 **1 自由度约束**，禁止自由 6DoF」。`src/interact/assembly.js` 逐条实现：

- 每件构件只有一个合法运动方向（`ASSEMBLY` 表，直接来自 §6）
- 错误方向 → 阻尼回弹 + 该步专属的引导文案
- 吸附阈值有难度梯度（B2 首次放宽，此后收紧）
- 连续 3 次未成功 → 放宽阈值并主动提供帮助（隐性辅助，不告知用户）
- **C11 立柱末段 2 mm 楔紧**：拖拽速度曲线压至 40%、阻力显著加大、未推到底则回弹 2 mm、该角枨框 0.3 mm 微沉

最后这条是本版最看重的手感设计：它把一个抽象的工程锁定原理，变成了可被手指感知的体验。

§7 表格里标注「⚠ 需自定义」的交互全部实现，且**每一项都内置了文档指定的降级路径**（点击自动装配 / 开始加工自动播放 / 三档按钮替代滑杆）。

---

## 7. 与原文档的已知差异

诚实列出：

| 项 | 原文档 | V3.0 | 原因 |
|---|---|---|---|
| 触觉反馈 | 全片大量 Haptics | 视觉微反馈 + 音频代偿，**时序保持原设计** | 桌面浏览器无振动 API |
| M4 真 AR | ARKit / ARCore + 光照估计 | WebXR 能力探测；桌面走**全景模式**（§7 既定降级路径） | 桌面 Chrome 无 AR Session。移动端探测到能力时可走真 AR |
| M3 笔顺动画 | 「须为真正的书写顺序」 | 逐字分段揭示 + 每段笔触声 | 真实笔顺需 60–80 字的 SVG path 序列，属内容交付物；代码已留出接入点 |
| M3 二维码 | ≥180×180 px | 海报上以文字呈现编号 | 不做假二维码。接真 QR 需引入唯一的外部依赖，留待产品定稿 URL 后加 |
| 龙纹/祥云纹样 | 写实雕花 | 程序化意象化处理（回旋脊线 / 云头曲线） | 无外部模型资产；结构职能与槽位对位完全按 J-5 / J-6 |
| 河姆渡构件 | 考古示意复原 | 带凸榫的做旧横梁段 | 同上。字幕已标注「示意复原」以规避学术争议 |

**这些差异不影响任何知识点与结构结论。**

---

## 8. 工程结构

```
src/
  core/       modulus.js   §3 模数体系（整数毫米 + 栅格断言）
              boxcsg.js    轴对齐 CSG 内核（贪心面网格化）
              parts.js     §4 BOM + §5 节点 → 13 件参数化构件
              verify.js    §13.1 自检表 → 运行时断言
              state.js     §13.3 全局状态（含 M3 隐私红线）
  render/     stage.js     渲染器 / 相机 / 光照 / 后期 / §1 镜头缓回
              materials.js §11.2 PBR + 程序化木纹（aCut 驱动加工感）
              lattice.js   格心三纹样（真实镂空棂条 + 投影贴图同源）
              decor.js     §4.3 非木构件（含 V-27 角花重设计）
              lantern.js   装配体中枢（两套互不复用的爆炸数据）
              fx.js        木屑 / 涟漪 / 能量环 / 烟花粒子
  interact/   assembly.js  §6 1 自由度约束装配（含楔紧段手感）
              machining.js 拖刀加工（含 §7 自动播放降级）
  audio/      sfx.js       模态合成音效引擎
              voice.js     §12.3 旁白与字幕（语速模型驱动）
              bgm.js       §12.1 曲目与分层
  ui/         hud.js       §12.5 UI 组件清单
  app/        engine.js    分步引擎
  steps/      act1/act3/act4.js  24 步内容
  modules/    m1-m2.js, m3-m5.js  五大互动模块
tools/        verify-geometry.mjs  Node 端几何验算（npm run verify）
              gen-voice.mjs / gen-bgm.mjs  MiniMax 生成管线
```

---

## 9. 验收

```bash
npm run verify   # 几何闭合验算，15/15 通过
npm run dev      # 本地开发
npm run build    # 静态产物 → dist/
```

页面加载时验算自动执行；界面右上角「校验」按钮可随时调阅完整报告。

> 若后续发现实现与几何仍有出入，**以 `src/core/verify.js` 的断言为准**——
> 它是这份文档里唯一会自己报错的部分。
