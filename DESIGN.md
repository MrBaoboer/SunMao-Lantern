# 实现说明

> 基准：《榫卯灯笼_国风流光_3D交互脚本 V2.0》——— 33 步主线 + M1–M5 五个模块 + 25 项几何校验。
> 本版把它落成一个能在浏览器里跑的网页，主线重构为 18 步。
> 目标平台：Chrome，桌面横屏优先，移动端响应式可用。

原稿是一份写给建模、动画、配音、UI 四支队伍的制作执行脚本。
这一版做的是同一件事的另一种解法：把其中可计算的部分交给代码，让几何自己证明自己是对的。

---

## 1. 几何：算出来的，不是建出来的

原稿 §3 建立了模数体系，§5 定义了五个节点的全部参数，§13.1 列了 15 条建模自检。
这三章合起来已经是一份可执行的规格，只是被写成了给人看的表格。这一版把它翻译成代码：

| 原稿 | 实现 |
|---|---|
| §3 模数体系 | `core/modulus.js` —— 全部尺寸为整数毫米（a/12 = 1 mm）。`a()` 带栅格断言，任何非模数尺寸当场抛错 |
| §5 节点参数 | `core/parts.js` —— 13 件木构件由「毛坯盒 − 若干带工序标签的切除盒」生成 |
| 布尔运算 | `core/boxcsg.js` —— 非均匀体素栅格 + 贪心面网格化。不做实时布尔，也天然没有内部面 |
| §13.1 自检表 | `core/verify.js` —— 15 条断言，页面加载即跑，控制台输出报告 |

由此换来三件事：

**加工感不用画。** CSG 内核给每个顶点标了 `aCut`（该面是否为加工新露出的面）。木料着色器据此在同一个材质里同时表达主表面与切面 —— 铣一刀，新切面自动亮一档。§11.2 要求的「切面必须比外表面浅一档」不需要美术单独出图。

**干涉检测是免费的。** 实体单元可以直接展开成不重叠的盒体集合，于是「17 件构件两两是否穿模」变成一次集合运算。当前结果：136 组配对，重叠体积全部为 0。

**面数很低。** 贪心面网格化之后，一根开满槽的顺枨 86 tris（§11.1 预算 900），17 件包络体合计 1152 tris（预算 50000）。省下的预算全给了格心镂空。

---

## 2. 两处几何裁决

原稿 §2 的 25 项修正（V-01 ~ V-25）本版全部复核通过。把每个尺寸代入整数坐标之后，又发现两处原稿内部的表述冲突，按几何自洽性作了裁决。

### V-26 · 中梁端部下半段：保留还是去除

- §5 J-2 承重面栏：「中梁端部**下半段保留为肩**，坐落在顺枨槽底余料上」
- S16 动画栏：「**切下半段肩**：自端部下半段横切**去除** a/2 高 × a/2 长的料」

代入坐标验算：顺枨开口槽只挖 z ∈ [2.5a, 3a]，槽底 z ∈ [2a, 2.5a] 是保留的余料。若中梁端部下半段也保留，两者在 y ∈ [3.5a, 4a] × z ∈ [2a, 2.5a] 正面干涉。

**裁决：下半段必须去除。** 横切后露出的水平面（z = 2.5a，即双榫底面）落在顺枨槽底上，这个面才是承重肩。文案相应改写为「端头下半段横切去除，切出的水平面即承重面」。

### V-27 · 龙纹角花够不到压槽

- §5 J-6：「角花内腔 a × a × **a/2** 方腔，套住柱头段**上部**」
- §5 J-6：「压舌 a/6 厚 × a/2 长，落入**上枨框顶面**对应浅槽」

柱头段高 2a（z 14a–16a），上枨框顶面在 z = 14a。若内腔只有 a/2 高且套住柱头上部，角花底面就在 z = 15.5a，压舌够不到 z = 14a 的槽，差 1.5a。
同时锁定原理也不成立：内腔四壁若与柱头同域，柱头沿 +X 外拔时并没有东西挡它。

**裁决：改为带外挡边的方形盖板。**

- 板贴上枨框顶面（z ∈ [14a, 14a + a/3]），中开方孔套住柱头根部
- 压舌自板底面向下 a/6，落入横枨顶面压槽
- 外缘出挑 a/6，正好横在柱头径向外拔的路径上

这样 §5 J-6 承诺的「结构副锁」才真正成立。`railB` 相应增加了 `PRESS_SLOT` 工序。

---

## 3. 主线：33 步 → 18 步

原稿 §8 的 33 步累加 908 秒，约 15 分钟。网页端 15 分钟线性流程的流失率很高，而流失最严重的恰恰是观看型步骤 —— 它们同时也最依赖高成本的写实美术资产。

反过来，程序化几何最擅长表达的，是 §2/§3/§5/§6 那套严丝合缝的模数结构。所以重构方向只有一句：**把「看视频」改成「看结构」。**

第一轮重构到 24 步。之后按「青少年能不能一口气看完」再收了一轮，砍掉的是：

- **两个静默点。** 「榫为阳，卯为阴」的抒情段与 12 秒无操作巡礼，对理解结构没有帮助，却是最容易让人退出的两处。
- **独立的「七千年」步。** 河姆渡构件是一次性的观看资产，事实本身并入「凸的叫榫，凹的叫卯」的第一句旁白。
- **重复的加工步。** 顺枨开槽与中梁开叉合成一步；切榫头与凿透眼合成一步；上框的加工与装配合成一步。都是同一批料上的连续工序，本来就不该断开。

旁白同时压掉了四成：2914 字 → 1718 字，约 8 分钟。

| 本版 | 内容 |
|---|---|
| **开场** | A1 一盏灯为一年收尾 · A2 成品亮相与爆炸预览 |
| **认识榫卯** | B1 榫与卯（含七千年、榫的三个部位、卯的三种规格）· B2 直榫 · B3 夹榫 |
| **做骨架** | C1 十三根木条 · C2 开槽与开叉 · C3 成「工」字 · C4 切榫头与凿透眼 · C5 底盘完成 · C6 上框（差别只有一处 + 装配）· C7 柱子开细颈 · C8 四柱合龙 |
| **装点年味** | D1 选纹样 · D2 装板 · D3 糊纸贴花上锁放灯芯 · D4 分层拆解 · D5 五个模块入口 |

保留下来的红线：C6 的对比动画（上框中部一刀不动）、D2 的三段式落堂装板、D3 首个角花的剖切演示、灯芯不提前点亮、片尾卡三行字、开场钩子与片尾闭环、语义色纪律。

### 不再拦人

原先每个动手步骤都设了闸：任务没做完，「继续」按钮就是灰的。这一版全部取消 ——

- 翻页在屏幕两侧，任何时候都能按；
- 顶部四章按步铺开，点哪一格跳哪一步；
- 旁白与画面同时开始，谁也不等谁；选项一出现就能点。

代价是用户可能跳过一次装配。但对一个自愿点进来的教学页面，「被拦住」的伤害远大于「少练一次」。

---

## 4. 教学件与刀具：两处「看起来是反的」

前三步的教学件原本是拿实心块拼出来的：卯眼是一块深色面片贴在木料表面上。
**贴上去的实体永远读成凸起** —— 它不透光、有自己的受光面，眼睛把它当成一块凸出来的黑块，
而这一步的全部意思恰恰是「凹进去的这一块叫卯」。

现在三个教学件（B1 榫与卯、B2 透榫、B3 夹榫）都走真构件那条管线：
`demoSolid()` 用毛坯减切除盒生成，内壁由 CSG 出面、照常受光，切面还按 §11.2 的规矩亮一档。
尺寸也就此与真节点对齐 —— B3 的槽宽、榫舌、槽深直接取 `J2`，一根 12 见方的料正好三等分。

刀具是另一处。三把刀都按「刃口朝 -Z、柄在 +Z、刀身沿 +X」建模，
但摆位用的是 `lookAt()` —— 它把物体的 **+Z** 转向目标，正好反过来：锯齿朝天，凿柄扎进木头。
更隐蔽的是，默认 up 是 +Z，而加工镜头几乎都是「朝下看」，这恰好是 `lookAt` 的退化情形，
滚转角由 three.js 内部的容错分支随手定，刀身还会歪。

改成直接搭一组正交基：`-Z → 攻角`，`+X → 走刀方向`，`Y = Z × X`。
`npm run smoke` 现在每一步都验一次「刃口与攻角的点积 ≥ 0.99」——
把基向量取反可以让它立刻报 `dot=-1.000`，这条断言不是摆设。

---

## 5. 交互：单自由度约束

§6 要求所有拖拽一律做成 1 自由度约束，禁止自由 6DoF。`interact/assembly.js` 逐条实现：

- 每件构件只有一个合法运动方向（`ASSEMBLY` 表直接来自 §6）
- 错误方向 → 阻尼回弹 + 该步专属的引导文案
- 吸附阈值有难度梯度：第一次放宽，此后收紧
- 连续 3 次未成功 → 放宽阈值并主动提供帮助，不告知用户
- C8 立柱末段 2 mm 楔紧（`J3.WEDGE_LEN`）：拖拽速度曲线压至 40%、阻力显著加大、未推到底则回弹 2 mm

最后一条是这一版最看重的手感设计：它把一个抽象的工程锁定原理，变成了手指能感觉到的东西。

§7 标注「需自定义」的交互全部实现。每一处动手的地方都并排放着一个不动手的出口 ——
装配旁是「帮我装上」（`drag.autoSeatAll()`），加工旁是「帮我加工」（`mach.autoRun()`）。
它们走的是同一段动画与同一组音效，跳过的只是手感，知识点一条不少。

---

## 6. 取景：算出来的，不是摆出来的

每一步都带一份机位声明：方位角、仰角、距离、目标点。距离原本是照着笔记本画幅一步步试出来的 ——
这在 16∶10 上成立，换个画幅就不成立了。

竖屏手机 390×844 的水平视场只有约 18°（垂直是 38°）。同一个 120 mm 宽的灯笼，
在笔记本上绰绰有余，在手机上直接裁掉两边。C7 那种贴脸镜头更糟：柱子上下都出画，
画面里只剩一段没有特征的木头，「三段两颈」这件事当场说不成立。

这一版把距离从「摆」改成「算」。每一步只声明**这一步必须完整看到多大一块**：

```js
cam: { az: 62, el: 12, dist: 470, fit: FIT_LANTERN }   // fit = { r: 98, h: 110 }
```

`stage.setRecommended()` 拿到 `fit` 后，水平与垂直两个方向各算一次装得下所需的距离，取远的那个，
再与作者写的 `dist` 取最大值 —— **只会往后退，不会往前凑**，宽屏上的取景意图原样保留。

界面占掉的画面也算进去。`hud` 实时量出常驻界面与坞的上下边界（`getClientRects`，
因为 fixed 定位的元素 `offsetParent` 恒为 null），交给 `stage.setSafeArea()`：

- 可用画幅按 `(高 − 上 − 下) / 高` 折算，取景据此再退一档（下限 0.58，界面再厚也不能把主体挤成邮票）；
- 主体按上下遮挡之差整体上抬，不会坐在字上；
- 画幅或界面高度一变，`resize` 会用同一份声明重算一遍。

同一条纪律也解决了底部叠字：坞摊开时把实测高度写进 `--dock-h`，
底部那一摞讲述与行动整体抬到坞上方（`padding-bottom: max(...)`）。
在此之前，D1 / D4 / D5 与三个模块的旁白都直接压在控件上。

---

## 7. 画面：曝光与背景

浅色主题下木料一度是奶白色的。原因是光加多了：环境 1.25 + 主光 2.2 + 补光 1.1 + 环境光 1.05，
四项叠起来越过了 ACES 的膝点，年轮与顺纹被一起推平；背景又恰好是同一档明度，主体于是与背景糊在一起。

两处调整：

- **总辐照压到约六成**（`MOODS.light.craft` 等）。木头重新看得出是木头，切面提亮那一档也重新可读。
- **背景不再是一块平色。** `makeBackdrop()` 在远平面画一块屏幕空间渐变：主体背后一圈暖光晕，四周压暗。
  这比加地面或假阴影都省，而且四种基调、两套主题共用一套实现 —— 每种基调只给一对颜色。

---

## 8. 结构检视器

原稿把剖切散落在 S07 / S11 / S17 / S19 / S24 / S27 六处，每处都是一次一次性演示。

因为几何是程序化的，这一版把它做成了常驻能力：任意时刻按 `X`，可对当前模型做爆炸与半透，两根滑杆连续控制。

这是网页端相对分步讲解器的真正优势 —— 不是「播放一段预制的剖切动画」，而是「随时可以拆开看」。

---

## 9. 音频

### 为什么音效是合成的

音效不能用现成采样，因为脚本要求它是参数化的：同一记咬合音要能升高两个半音，十三次落料要能随机浮动，四根立柱到位要依次上行，走刀音要随刀数升高。

`audio/sfx.js` 用模态合成实现，方法是物理导向的：敲击 = 一个宽带瞬态 + 一组指数衰减的共振模态；锯、凿、铣是带通噪声加包络。木料模态阻尼大、余韵短（≤0.3 s）。

### 一次减法

第一版做了 60 多个音效，结果是每一次点击、每一次切换、每一次高亮都在响。走完一遍主线，听到的提示音比木头声还多。

这一版把音效表收到 20 个，只留有物理来由的声音：

| | |
|---|---|
| 木作 | 咬合、楔紧、轻敲、落料、坐实、滑动、装板 |
| 刀具 | 锯、凿、铣、木屑 |
| 纸与装饰 | 纸、笔触、结与流苏 |
| 火 | 点亮、燃烧 |
| 界面 | 选中、做完一件事、快门 —— 只有这三记 |

删掉的是各种「叮」「唰」「嗡」：UI 提示音、成就音、光效音、扫描音、时间轴音。它们没有物理来由，也不构成信息 —— 界面上已经能看见的事，不需要再响一声。

旧编号没有直接删除：有物理来由的并到新音上（`ALIASES`），纯提示性的进 `MUTED` 集合。名字留在那儿，一眼能看出是被有意去掉的，而不是漏了。


### 配音与 BGM

音频层与生成工具解耦：播放端只认目录与清单。

- 旁白 → `public/audio/vo/{编号}.mp3` ＋ 同目录 `manifest.json`：`{ "ids": [...] }`
- 音乐 → `public/audio/bgm/{曲名}.mp3` ＋ `{ "files": [...] }`

全片旁白见 [旁白解说稿.md](旁白解说稿.md)，由 `tools/make-script.mjs` 从运行时的真实步骤数据导出，因此与线上逐字一致，不会因源码改动而失同步。

没有音频时，字幕按语速模型独立走完同一套内容。

---

## 10. 界面

设计语言、令牌与组件另见 [UI.md](UI.md)。四条要点：

**先有一道门。** 原先加载完就直接落进第一步：用户既没准备好，浏览器的音频也还没解锁（自动播放策略要求一次真实手势）。这一版把加载页做成封面 —— 三维就绪后实底化开，背后就是那盏做好的灯。按下「开始做灯」同时完成三件事：淡出、解锁音频、进入。

**每次打开都是新的一遍。** 状态分两类：深色、声音、字幕这些「这台设备上我习惯怎么用」跨会话留着；做到哪一步、灯亮没亮、猜对几题，刷新即归零。做一遍只要八分钟 —— 半截存档换来的是一次「你上次停在……」的提问，而这个问题出现在你刚打开页面、还没想好要不要玩的时候，是纯粹的干扰。封面因此只有一个入口。

**先说清楚怎么玩。** 三维拖拽不是不学就会的事。第一次进来先给四条操作，之后不再出现；右上角常留一个六条的完整版。

**翻页在两侧。** 底部原来那个「继续」按钮取消了。左右两枚箭头贴着屏幕边缘，像翻书 —— 底部于是只剩讲述和这一步的任务，画面中央始终留给灯笼。窄屏下这两枚落到底部两角，把屏幕两侧整片让给手指转动画面。

**浅色是默认。** 工作台的光跟着主题走：浅色模式下是明亮的宣纸桌面，深色模式下是暖调的暗房。夜里的场景（D5 与其后的互动模块）不跟主题走 —— 灯笼只有在暗处才亮得起来，那几步界面会临时借用深色，用户的选择不受影响。

---

## 11. 与原稿的已知差异

| 项 | 原稿 | 本版 | 原因 |
|---|---|---|---|
| 触觉反馈 | 全片大量 Haptics | 视觉微反馈 + 音频代偿，时序保持原设计 | 桌面浏览器无振动 API |
| M4 真 AR | ARKit / ARCore + 光照估计 | 程序化全景背景，可在其中挂多盏并拍照（§7 既定降级路径） | 未接入 WebXR。全景模式在桌面与手机上表现一致，也不需要相机权限 |
| M3 笔顺动画 | 须为真正的书写顺序 | 逐字分段揭示 + 每段笔触声 | 真实笔顺需 60–80 字的 SVG path 序列，属内容交付物；代码已留接入点 |
| M3 二维码 | ≥180×180 px | 海报上以文字呈现编号 | 不做假二维码。接真 QR 需引入唯一的外部依赖，留待产品定稿 URL 后加 |
| 龙纹 / 祥云纹样 | 写实雕花 | 程序化意象化处理（回旋脊线、云头曲线） | 无外部模型资产。结构职能与槽位对位完全按 J-5 / J-6 |
| 河姆渡构件 | 独立一步的考古示意复原 | 只留一句旁白 | 一次性观看资产，收益不抵它占掉的时间 |
| 格心纹样 | 麻叶 / 冰裂 / 万字 三选一 | 麻叶 / 万字 两选一 | 冰裂纹的递归剖分在小尺寸格心上读不出「线不相交」，反而像随机网格 |

这些差异不影响任何知识点与结构结论。

---

## 12. 目录

```
src/
  core/       modulus.js   模数体系（整数毫米 + 栅格断言）
              boxcsg.js    轴对齐 CSG 内核（贪心面网格化）
              parts.js     BOM 与节点 → 13 件参数化构件
              verify.js    15 条运行时几何断言
              state.js     全局状态（含写心愿模块的隐私红线）
  render/     stage.js     渲染器 / 相机 / 光照 / 后期 / 镜头缓回
              materials.js PBR + 程序化木纹（aCut 驱动加工感）
              lattice.js   格心两种纹样（镂空棂条与投影贴图同源）
              decor.js     非木构件（含 V-27 角花重设计）
              lantern.js   装配体中枢（两套互不复用的爆炸数据）
              fx.js        木屑 / 涟漪 / 能量环
  interact/   assembly.js  单自由度约束装配（含楔紧段手感）
              machining.js 拖刀加工 —— 刀具建模约定与正交基摆位
  audio/      sfx.js       模态合成音效引擎
              voice.js     旁白与字幕（语速模型驱动）
              bgm.js       曲目与分层
  ui/         hud.js       组件层
              icons.js     线稿图标
              styles/      令牌与样式分层
  app/        engine.js    分步引擎
  steps/      act1/act3/act4.js  18 步内容
  modules/    m1-m2.js, m3-m5.js  四个互动模块
              vo.js        模块旁白的唯一出处（播放与导稿共用）
tools/        verify-geometry.mjs  Node 端几何验算
              smoke.mjs            冒烟测试（无头浏览器走完主线）
              make-script.mjs      从运行时数据导出旁白解说稿
```

---

## 13. 验收

```bash
npm run verify   # 几何闭合验算（Node），15 条断言
npm run build    # 静态产物 → dist/
npm run smoke    # 冒烟测试：无头浏览器在两种画幅下走完十八步
npm run check    # 上面三样按顺序跑一遍
```

两道验证各管一半：

- `verify` 证明几何是闭合的 —— 尺寸落在模数栅格上、透眼真的贯穿、17 件构件两两无干涉、面数在预算内。它跑在 Node 里，不需要浏览器。
- `smoke` 证明这些几何真的能在页面上跑起来 —— 十八步全部可达、控制台无报错、无资源缺失、相机距离正常、**刀具刃口朝着工件**、五个模块入口都在、深色能切。1440×900 与 390×844 各走一遍。

页面加载时验算也会自动执行一遍，右上角「尺寸对照」可随时调阅。
若后续发现实现与几何有出入，以 `src/core/verify.js` 的断言为准 —— 它是这份文档里唯一会自己报错的部分。
