# 实现说明

> 基准：《榫卯灯笼_国风流光_3D交互脚本 V2.0》—— 33 步主线 + M1–M5 五个模块 + 25 项几何校验。
> 目标平台：桌面横屏优先；平板、手机竖屏与横屏各有一套布局。浏览器基线见 [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md#环境)。

原稿是一份写给建模、动画、配音、UI 四支队伍的制作执行脚本。这一版把它落成一个能在浏览器里跑的网页，
主线重构为 18 步，可计算的部分全部交给代码 —— 让几何自己证明自己是对的。

---

## 1. 几何：算出来的，不是建出来的

原稿 §3 的模数体系、§5 的五个节点参数、§13.1 的 15 条建模自检，合起来已经是一份可执行的规格，
只是写成了表格。这一版把它翻译成代码：

| 原稿 | 实现 |
|---|---|
| §3 模数体系 | `core/modulus.js` —— 全部尺寸为整数毫米（a/12 = 1 mm）。`a()` 带栅格断言，任何非模数尺寸当场抛错 |
| §5 节点参数 | `core/parts.js` —— 13 件木构件由「毛坯盒 − 若干带工序标签的切除盒」生成 |
| 布尔运算 | `core/boxcsg.js` —— 非均匀体素栅格 + 贪心面网格化。不做实时布尔，也天然没有内部面 |
| §13.1 自检表 | `core/verify.js` —— 17 条断言（原 15 条 + 走刀局部性 + 工序有效性），页面加载即跑，控制台输出报告 |

由此换来三件事：

**加工感不用画。** CSG 给每个顶点标了 `aCut`（该面是否为加工新露出的面），
木料着色器据此在同一个材质里同时表达主表面与切面。§11.2 要求的「切面比外表面浅一档」因此不需要美术单独出图。

**木纹长在木头上。** 年轮与顺纹在**构件自己的坐标系**里算（几何已按毛坯中心归位，原点就是年轮之心），
不是世界坐标 —— 否则料一被拖动、离位陈列或爆炸拆开，纹理就在木头里流过去；转过来摆的料更糟，
顺纹方向按世界轴取，一根横放的料会变成横纹。十三根仍要各有各的花，靠的是 `seed`（只搅动噪声的取样点）
与 `tone`（整体色差），不是位置。

**干涉检测不用另写。** 实体单元可以直接展开成不重叠的盒体集合，
「17 件构件两两是否穿模」于是变成一次集合运算。当前结果：136 组配对，重叠体积全部为 0。

**面数很低。** 贪心面网格化之后，一根开满槽的顺枨 86 tris（预算 130），17 件包络体合计 1152 tris。
每件的预算按实测值留四到七成余量（`parts.js` 的 `budget`），合计上限 1400，省下的部分全给了格心镂空。

---

## 2. 两处几何裁决

原稿 §2 的 25 项修正（V-01 ~ V-25）本版全部复核通过。把每个尺寸代入整数坐标之后，
另有两处原稿内部的表述冲突，按几何自洽性作了裁决。

### V-26 · 中梁端部下半段：保留还是去除

- §5 J-2 承重面栏：「中梁端部**下半段保留为肩**，坐落在顺枨槽底余料上」
- S16 动画栏：「**切下半段肩**：自端部下半段横切**去除** a/2 高 × a/2 长的料」

代入坐标验算：顺枨开口槽只挖 z ∈ [2.5a, 3a]，槽底 z ∈ [2a, 2.5a] 是保留的余料。
若中梁端部下半段也保留，两者在 y ∈ [3.5a, 4a] × z ∈ [2a, 2.5a] 正面干涉。

**裁决：下半段必须去除。** 横切后露出的水平面（z = 2.5a，即双榫底面）落在顺枨槽底上，
这个面才是承重肩。文案相应改写为「端头下半段横切去除，切出的水平面即承重面」。

### V-27 · 龙纹角花够不到压槽

- §5 J-6：「角花内腔 a × a × **a/2** 方腔，套住柱头段**上部**」
- §5 J-6：「压舌 a/6 厚 × a/2 长，落入**上枨框顶面**对应浅槽」

柱头段高 2a（z 14a–16a），上枨框顶面在 z = 14a。若内腔只有 a/2 高且套住柱头上部，
角花底面就在 z = 15.5a，压舌够不到 z = 14a 的槽，差 1.5a。同时锁定原理也不成立：
内腔四壁若与柱头同域，柱头沿 +X 外拔时并没有东西挡它。

**裁决：改为带外挡边的方形盖板。**

- 板贴上枨框顶面（z ∈ [14a, 14a + a/3]），中开方孔套住柱头根部
- 压舌自板底面向下 a/6，落入横枨顶面压槽
- 外缘出挑 a/6，正好横在柱头径向外拔的路径上

这样 §5 J-6 承诺的「结构副锁」才真正成立。`railB` 相应增加了 `PRESS_SLOT` 工序。

---

## 3. 主线：33 步 → 18 步

原稿 §8 的 33 步累加 908 秒，约 15 分钟。撑起这个时长的多是观看型步骤，而它们又最依赖高成本的写实美术资产；
程序化几何最擅长表达的，恰恰是 §2/§3/§5/§6 那套严丝合缝的模数结构。所以重构方向只有一句：**把「看视频」改成「看结构」。**

砍掉的是：

- **两个静默点。** 「榫为阳，卯为阴」的抒情段与 12 秒无操作巡礼，对理解结构没有帮助，却是最容易让人退出的两处。
- **独立的「七千年」步。** 河姆渡构件是一次性的观看资产，事实本身并入「凸的叫榫，凹的叫卯」的第一句旁白。
- **重复的加工步。** 顺枨开槽与中梁开叉合成一步；切榫头与凿透眼合成一步；上框的加工与装配合成一步。
  都是同一批料上的连续工序，本来就不该断开。

旁白同时压掉近四成：2914 字 → 1798 字，其中主线十八步 1580 字、念下来约七分半。
这两个数由 `npm run script` 从源码现算。

| 本版 | 内容 |
|---|---|
| **开场** | A1 一盏灯为一年收尾 · A2 成品亮相与爆炸预览 |
| **认识榫卯** | B1 榫与卯（含七千年、榫的三个部位、卯的三种规格）· B2 直榫 · B3 夹榫 |
| **做骨架** | C1 十三根木条 · C2 开槽与开叉 · C3 成「工」字 · C4 切榫头与凿透眼 · C5 底盘完成 · C6 上框（差别只有一处 + 装配）· C7 柱子开细颈 · C8 四柱合龙 |
| **装点年味** | D1 选纹样 · D2 装板 · D3 糊纸贴花上锁放灯芯 · D4 分层拆解 · D5 四个模块入口 |

保留下来的红线：C6 的对比动画（上框中部一刀不动）、D2 的三段式落堂装板、D3 首个角花的剖切演示、
灯芯不提前点亮、片尾卡三行字、开场钩子与片尾闭环、语义色纪律。

主线不等人：跳章与旁白任何时候都能按。需要动手的步骤上，「下一步」先用来做活儿 ——
一下按键做一段，做完最后一段才翻页。规矩本身见 [UI.md](UI.md#五条规矩)。

---

## 4. 教学件与刀具：让动作和结果对得上

三个教学件（B1 榫与卯、B2 透榫、B3 夹榫）都走真构件那条管线：`demoSolid()` 用毛坯减切除盒生成，
内壁由 CSG 出面、照常受光，切面按 §11.2 的规矩亮一档。尺寸也与真节点对齐 —— B3 的槽宽、榫舌、
槽深直接取 `J2`，一根 12 见方的料正好三等分。

**凹进去的地方必须真的没有料。** 贴一块深色面片假装卯眼，得到的一定是相反的读数：它不透光、
有自己的受光面，眼睛把它读成一块凸起 —— 而这一步的全部意思恰恰是「凹进去的这一块叫卯」。

刀具按同一个约定建模：**刃口朝 -Z、柄在 +Z、刀身沿 +X**，
摆位时直接搭一组正交基（`-Z → 攻角`，`+X → 走刀方向`，`Y = Z × X`）。不要改用 `lookAt()`：
它转的是物体的 **+Z**，会把锯齿转到朝天；而且默认 up 是 +Z，加工镜头几乎都是「朝下看」，
正好是它的退化情形，滚转角由 three.js 内部的容错分支随手定，刀身还会歪。
`npm run smoke` 每一步都验一次「刃口与攻角的点积 ≥ 0.99」—— 把基向量取反会立刻报 `dot=-1.000`。

**刀是一层层沉下去的，不是一开始就埋在料里。** 走刀线给的是刀走到底时刃尖所在的那条线；
直接把刀摆上去，第一刀还没去料，凿身就已经插在实料中间了。所以 `Machining.begin()` 按进刀轴
量一次工件的表面（`carve.parts` 的包围盒），开工时刃尖落在表面上，再随去料进度 `carveT` 沉到刃线 ——
刃尖始终踩在当前的切削面上，与去料用的是同一个进度，不会比料先到。

### 料要跟着刀走

「工序」是一个布尔开关，走刀是一段连续的运动。`buildPart(id, ops, carve)` 把两者接上：
`carve` 带着刃尖位置、进给轴、进刀轴与进度 `t`，据此现算这一刀啃掉的那一块。

- **这一刀压在哪个盒上。** 判据是**既非进给轴、也非进刀轴的那第三个轴**：刃尖必须落在切除盒的范围内。
  进给轴由 `to − from` 定，进刀轴由 `faceNormal` 定，两者指同一个轴时判据失效。
  顺枨顶面两条槽因此要走两趟，一趟一条。
- **啃到多深。** 自入刀面向里推进 `t = (已完成刀数 + 本刀走过的比例) / 总刀数`。一刀一层，
  这正是凿的去料方式；只增不减，手往回拖木头不会长回去。
- **扫到哪儿。** 进给轴上按刃尖真正走过的区间裁一刀。连续的长槽是一点点变长再变深，
  两个分开的孔则是刀经过谁、谁才开始掉料。走刀线要完整跨过它要开的那几处 —— 起点落在孔里，
  孔的外半边就永远扫不到。
- **走完的那几趟要连区间一起记。** 同一道工序走好几趟时，走完的要记在构件上，否则开第二条槽时第一条会长回去。
  但只记刃尖位置不够：第一条判据刻意不看进给轴，横截位置一模一样、只沿走刀方向分开的两处料
  （横枨的两个透眼、两个柱窝）会被前一趟的记录一并认领，第二个孔在刀落下之前就已经通了。
  所以 `carveFinish()` 同时记下那一趟刃尖**扫过的区间**，`laneCovers()` 用它做交集判断。
  `verify.js` 的 `[CARVE]` 钉住这条：第一趟走完 → 另一处必须还是实心；第二趟刚下刀 → 第一处不许长回去、第二处不许提前成形。
- **一处一趟刀，一道工序一条走刀线。** 透眼与柱窝各有两个，一个孔一趟，走刀线只跨过那个孔本身；
  通长的装板槽仍是一刨到底。中梁那三道各给各的：截短自顶面横跨料宽，开叉口自端面顺着槽往里剔，
  切承重面自端面横着推 —— 叉口只有 4 mm 宽，共用「横跨料宽」那条线，刀会在料外飞出两个身位；
  承重面在端头的**下半段**，从顶面进刀等于要先穿过上半段还在的料。攻角、进给方向、机位都跟着各自的活走。

几何按 1/32 量化重建 —— 这些构件贪心网格化之后只有几十到一百多个三角形，重建一次比画一帧还便宜。

**工具只有锯、凿、刨三样。** 凿是单面斜磨的剖面立起来挤出，刃宽 4 mm，与它要凿的那道槽同宽；
柄是八棱的，带挡铁与敲击铁箍。锯齿距 1.5 mm，柄尾端上翘。柱窝与细颈本来就是凿的活，装板槽是刨的活。
不用电动铣刀 —— 那么大一个机身杵在 12 mm 见方的料旁边，读出来是台角磨。

---

## 5. 交互：单自由度约束

§6 要求所有拖拽一律做成 1 自由度约束，禁止自由 6DoF。`interact/assembly.js` 逐条实现：

- 每件构件只有一个合法运动方向（`ASSEMBLY` 表直接来自 §6）
- 错误方向 → 阻尼回弹 + 该步专属的引导文案
- 主线吸附阈值 5–6 mm；教学件走轻量的 `simpleDrag`，不设毫米阈值，推过全程 88% 即坐实
- 连续 3 次未成功 → 悄悄把阈值放宽到 12 mm，并主动提供帮助（有楔紧段的那一步不放宽，否则末段回弹又变回不可达）
- C8 立柱末段 2 mm 楔紧（`J3.WEDGE_LEN`）：拖拽速度曲线压至 40%、阻力显著加大、未推到底则回弹 2 mm。
  这一档必须排在吸附判定**之前** —— 两条门槛互相包含，写在后面就是死代码

每一处动手的地方都并排放着一个不动手的出口：装配是 `drag.autoSeatAll()`（「帮我装上」），
加工是 `mach.autoRun()`（「帮我加工」）。两条路走同一段动画与同一组音效。
按「下一步」时引擎走的也是这两条，一下按键做一段（走刀一趟、装一件），
见 [ARCHITECTURE.md](docs/ARCHITECTURE.md#一步长什么样)。

---

## 6. 取景：算出来的，不是摆出来的

按笔记本画幅试出来的机位换个画幅就不成立。竖屏手机 390×844 的水平视场只有约 18°（垂直 38°）：
同一个 120 mm 宽的灯笼在笔记本上绰绰有余，在手机上直接裁掉两边；C7 那种贴脸镜头更糟，柱子上下都出画，
「三段两颈」这件事当场说不成立。

所以每一步只声明**这一步必须完整看到多大一块**，距离由代码算：

```js
cam: { az: 62, el: 12, dist: 470, fit: FIT_LANTERN }   // fit = { r: 98, h: 110 }
```

`stage.setRecommended()` 拿到 `fit` 后，水平与垂直两个方向各算一次装得下所需的距离，取远的那个，
再与作者写的 `dist` 取最大值 —— **只会往后退，不会往前凑**，宽屏上的取景意图原样保留。

界面占掉的画面也算进去。`hud` 实时量出常驻界面与坞的上下边界（用 `getClientRects`，
因为 fixed 定位元素的 `offsetParent` 恒为 null），交给 `stage.setSafeArea()`：

- 可用画幅按 `(高 − 上 − 下) / 高` 折算，取景据此再退一档（下限 0.62，界面再厚也不能把主体挤成邮票）；
- 主体按上下遮挡之差整体上抬，不会坐在字上；
- 画幅或界面高度一变，`resize` 会用同一份声明重算一遍。

**取景要把刀也算进去。** 工件是 12 mm 见方的一根料，凿自刃口向上还有 43 mm 刀身；底部界面又比顶部厚，
安全区的抬升再把主体往上推一档 —— 只按工件声明 `fit`，刀柄会顶穿画面上沿。
`AIM_BENCH` 把目标点抬起 16 mm、`FIT_BENCH` 按「刀 + 工件」给高度，每一趟走刀都有自己的机位，刀整根在画面内。

**用户转过的画面不许被界面推走。** `setRecommended()` 会清掉「用户接管过」这个标记，
而底部提示一换字数，`ResizeObserver` 就会经 `setSafeArea()` 绕到它 —— 内部重算因此要传 `{ keepUser: true }`：
那只是拿旧声明重算距离，不是新的取景意图。即便标记保住了，安全区一变推荐机位本身也会挪，
所以动手的步骤里 `stage.hold(true)` 连这次重算一起冻住。

**小变化一律不重新取景**（`REFRAME_MIN = 48 px`，与**上一次真正据以取景的那一组**比，不与上一帧比）。
字幕一句一行、两行来回换，安全区就跟着差一行的高度；每变一次就重算一次机位的话，画面每隔几秒
轻轻挪一下 —— 相机在飘，主光的目标点跟着飘，阴影贴图随之整体位移，纸面上棂条的影子持续爬动，
看上去正是「东西明明停着，却在轻微颤动」。一行字不过占画面高度的百分之三，那点位移换不来任何取景上的好处；
坞这一级的变化（上百像素）照样重新取景。

界面自己怎么给三维让位，见 [UI.md](UI.md#底部)。

---

## 7. 画面：曝光与背景

浅色主题四路光的总辐照（`MOODS.light.*`）只留了约三成余量：再上调就会越过 ACES 的膝点，
年轮与顺纹被一起推平，木头看起来是奶白色的，切面提亮那一档也跟着不可读。想让画面更亮，
优先调背景与 bloom。

背景不是一块平色：`makeBackdrop()` 在远平面画一块屏幕空间渐变，主体背后一圈暖光晕，四周压暗。
这比加地面或假阴影都省，四种基调、两套主题共用一套实现 —— 每种基调只给一对颜色。

**灯笼是挂着的，地面就该在灯脚之下。** 地面沉到 z = −30，穗子才垂得下 26 mm（约合灯身的七分之一）——
贴在灯脚下面的地面只容得下 24 mm，再长就埋进地板。投射距离随之变长，地面那片纹样光斑也摊得开。

**绵纸按真实读数给不透明度。** 反射光下基本不透（0.86），点亮后再实一点（0.96），透光交给 `emissiveIntensity`。
按「半透明」的直觉压到 0.55，不点灯时就能一眼看穿到对面那片格心，两层万字纹叠在一起，整盏灯读出来是个玻璃罩子 ——
而封面、开场、装板、分层拆解这些要看清木作的画面几乎全是不点灯的。棂条压在发亮的纸上，剪影反而更清楚。

---

## 8. 结构检视器

原稿把剖切散落在 S07 / S11 / S17 / S19 / S24 / S27 六处，每处都是一次性演示。几何既然是程序化的，
这一版就把它做成常驻能力：任意时刻按 `X`，对当前模型做爆炸与半透，两根滑杆连续控制。

---

## 9. 音频

### 为什么音效是合成的

脚本要求音效是参数化的：同一记咬合音要能升高两个半音，十三次落料要能随机浮动，
四根立柱到位要依次上行，走刀音要随刀数升高。现成采样做不到。

`audio/sfx.js` 用模态合成实现，方法是物理导向的：敲击 = 一个宽带瞬态 + 一组指数衰减的共振模态
（模态比取木条自由-自由弯曲的近似值 1 : 2.76 : 5.4 : 8.9）；锯、凿、刨是带通噪声加包络。
木料模态阻尼大、余韵短（≤0.3 s）。

### 只留有物理来由的声音

音效表收在 20 个：

| | |
|---|---|
| 木作 | 咬合、楔紧、轻敲、落料、坐实、滑动、装板 |
| 刀具 | 锯、凿、刨、木屑 |
| 纸与装饰 | 纸、笔触、结与流苏 |
| 火 | 点亮、引火、燃烧（后两记是循环音） |
| 界面 | 选中、做完一件事、快门 —— 只有这三记 |

各种「叮」「唰」「嗡」（UI 提示音、光效音、剖切与分层音、时间轴音）不在其列：它们没有物理来由，
也不构成信息 —— 界面上已经能看见的事，不需要再响一声。这些编号收在 `MUTED` 里，调了不响；
有物理来由的那些由 `ALIASES` 折到这 20 记上。名字一律留着，好让人看出它是有意去掉的，不是漏了。

### 配音与 BGM

音频层与生成工具解耦，播放端只认目录与清单：

- 旁白 → `public/audio/vo/{编号}.mp3` ＋ 同目录 `manifest.json`：`{ "ids": [...] }`
- 音乐 → `public/audio/bgm/{曲名}.mp3` ＋ `{ "files": [...] }`

全片旁白见 [旁白解说稿.md](旁白解说稿.md)，由 `tools/make-script.mjs` 从运行时的真实步骤数据导出，
与线上逐字一致。没有音频时，字幕按语速模型独立走完同一套内容。

---

## 10. 界面

设计语言、令牌、组件、主题与文案规则见 [UI.md](UI.md)。

与三维的接口只有一处：每一步声明的 `mood` 交给 `stage.setMood()`，它同时决定四路光与背景的基调、
以及界面要不要临时借用深色（`stage.onMood` → `hud.setTone()`）。曝光的上限见 §7。

---

## 11. 与原稿的已知差异

| 项 | 原稿 | 本版 | 原因 |
|---|---|---|---|
| 触觉反馈 | 全片大量 Haptics | 视觉微反馈 + 音频代偿，时序保持原设计 | 桌面浏览器无振动 API |
| M4 真 AR | ARKit / ARCore + 光照估计 | 程序化全景背景，可在其中挂多盏并拍照（§7 既定降级路径） | 未接入 WebXR。全景模式在桌面与手机上表现一致，也不需要相机权限 |
| M3 笔顺动画 | 须为真正的书写顺序 | 逐字分段揭示 + 每段笔触声 | 真实笔顺需 60–80 字的 SVG path 序列，属内容交付物；代码已留接入点 |
| M3 二维码 | ≥180×180 px | 海报上以文字呈现编号 | 不做假二维码。接真 QR 需引入唯一的外部依赖，留待产品定稿 URL 后加 |
| 龙纹 / 祥云纹样 | 写实雕花 | 程序化意象化处理（回旋脊线、云头曲线） | 无外部模型资产。结构职能与槽位对位完全按 J-5 / J-6 |
| 格心纹样 | 麻叶 / 冰裂 / 万字 三选一 | 麻叶 / 万字 两选一 | 冰裂纹的递归剖分在小尺寸格心上读不出「线不相交」，反而像随机网格 |
| M5 放烟花 | 独立一个模块 | 撤掉，四扇门只剩 M1–M4 | 它与这盏灯没有任何结构或知识关系，只是一段粒子表演；片尾接在「四件事都做完」之后，落点反而更实 |

---

## 12. 验收

命令见 [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md#命令)，CI 见 [ARCHITECTURE.md](docs/ARCHITECTURE.md#检查)。两道验证各管一半：

- `verify` 证明几何是闭合的 —— 尺寸落在模数栅格上、透眼真的贯穿、17 件构件两两无干涉、面数在预算内。跑在 Node 里，不需要浏览器。
- `smoke` 证明这些几何真的能在页面上跑起来 —— 十八步全部可达、每一步画面里确实有东西（可见且落在画幅内的
  网格数 ≥ 2）且不是一块纯色、控制台无报错、无 HTTP 4xx/5xx、相机距离正常、刀具刃口朝着工件、
  加工与装配的降级路径走得完、需要动手的一步按「下一步」是一下做一段、做完才翻页、C6 一键六道工序、
  M1–M4 四个模块各走一遍、深浅两套主题都真的换过去。1440×900 与 390×844 各走一遍，
  **桌面画幅再倒着走一遍**（正着走时每一步都由上一步替它铺好了台子，「这一步自己没把台子摆全」那一类
  毛病一条都看不见）。

页面加载时验算也会自动执行一遍，右上角「尺寸对照」可随时调阅。
若实现与几何有出入，以 `src/core/verify.js` 的断言为准 —— 规格里只有它会自己报错。
